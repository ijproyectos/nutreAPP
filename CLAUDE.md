# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

**Fase 6: todo lo de la Fase 5 (login en vivo, shell/dashboard/pacientes, laboratorios, plan con IA) + listo para deploy en Netlify.** `apps/web/` es Next.js 16 (Turbopack, App Router) — build y lint limpios. shadcn/ui en estilo `base-nova` (`@base-ui/react`, no Radix — composición con `render` prop, no `asChild`). `netlify.toml` en la raíz (mismo patrón monorepo de Raíces del Sur — `base = "apps/web"`, `publish = ".next"` explícito). Falta: crear el sitio en Netlify, conectar el repo de GitHub y cargar las 4 env vars ahí — eso es acción del usuario, no verificable desde acá.

**Laboratorios clínicos** (agregado post-v1, no estaba en el PRD original — ver `docs/architecture.md` §9 y `docs/requirements.md` RF-084/085/090/091 para el detalle):
- `supabase/migrations/004_laboratorios.sql` — tabla `laboratorios` + RLS + bucket privado de Storage (`laboratorios`) + policies de `storage.objects`. **Aplicada y verificada** (mismo mecanismo `psql`/pooler).
- `src/lib/laboratorios/parsear.ts` — extracción de texto de PDF con `unpdf` (sin OCR, sin dependencias nativas) + regex simples por analito. Nunca lanza — si falla, `valores = {}`.
- `src/app/portal/laboratorios/` — el paciente sube el archivo (Server Action `subirLaboratorio`: upload a Storage + insert + intento de parseo, todo en un solo paso; subió `experimental.serverActions.bodySizeLimit` a 15mb en `next.config.ts` porque el default de 1MB no alcanza para un PDF/foto) y ve su propio listado con estado.
- `src/app/app/pacientes/[id]/` — **ficha de paciente nueva** (no existía antes de esto — RF-022 solo estaba especificada, no construida). Tiene las secciones Historia clínica, Laboratorios (revisar/editar valores/validar/rechazar) y Plan alimentario (ver abajo).
- Bandeja de hoy: nueva alerta (`src/lib/queries/laboratorios.ts`) — laboratorios `pendiente_revision` hace más de 48hs.
- Trade-off de RLS documentado (no un bug): el paciente puede reescribir `notas_profesional` de su propio laboratorio mientras siga pendiente — ver la nota en `004_laboratorios.sql` y en `docs/architecture.md` §9 antes de "corregirlo" sin releer por qué se aceptó así.

**Generación de plan con IA** (`docs/architecture.md` §10, `docs/requirements.md` RF-062/063):
- `supabase/migrations/005_planes_ia.sql` — `planes` ganó `estado` (`borrador_ia`/`editado_manual`/`enviado`, default `editado_manual`), `generado_con_ia`, `laboratorio_id`. **Aplicada y verificada.**
- `src/lib/ai/generar-plan.ts` — único punto de contacto con la API de Anthropic (`@anthropic-ai/sdk`, `ANTHROPIC_API_KEY` en `.env.local`, `import "server-only"`). Modelo `claude-opus-5`. Usa `client.messages.parse()` con schema Zod (`output_config.format`) para forzar salida estructurada — nunca texto libre sin validar — y esa estructura se convierte a markdown antes de guardarse en `planes.contenido` (no hay columna jsonb paralela). **Verificado contra la API real** (no solo compilado): confirmé que el plan generado efectivamente ajusta según los valores de laboratorio pasados, no solo que la llamada no tira error.
- `src/app/app/pacientes/[id]/planes-actions.ts` + `plan-ia-panel.tsx` — "Generar plan con IA" (usa el laboratorio **validado** más reciente, nunca uno pendiente/rechazado) → `borrador_ia`; el profesional edita el texto libremente y "Guardar borrador" (`editado_manual`) o "Enviar al paciente" (`enviado`, recién ahí lo ve el paciente). Ningún camino de código salta directo a `enviado` sin pasar por esta pantalla.
- `src/app/portal/plan/page.tsx` — RF-061, el paciente ve su plan vigente (la policy RLS `planes_select_paciente` ya filtraba por `enviado_at is not null` desde antes de que existiera IA).
- Ya no hay gap: además de "Generar plan con IA", la ficha tiene "Escribir manualmente" (`crearPlanManual` en `planes-actions.ts`) — crea el plan directo en `editado_manual`, sin pasar por la IA (RF-060).
- Bug real corregido (reportado por el usuario: "genero el plan y no pasó nada"): `PlanIAPanel` es client component con `useState(planActivo?.contenido ?? "")` para el texto editable. Sin un `key` en el `<PlanIAPanel>` de `page.tsx`, React reusaba la misma instancia al pasar de "sin plan" a "con plan recién generado" — la acción sí insertaba el plan y `revalidatePath` sí traía el `planActivo` nuevo, pero el `useState` local quedaba pisado en `""` (su valor inicial, que solo se lee en el mount), así que el textarea se veía vacío y parecía que no había pasado nada. Fix: `key={planActivo?.id ?? "sin-plan"}` en `page.tsx` para forzar remount cuando cambia la identidad del plan.

**Historia clínica** (RF-022, completa la ficha de paciente — pedido explícito del usuario, antes solo estaban Laboratorios y Plan):
- `supabase/migrations/007_historia_clinica.sql` — agrega `pacientes.notas_generales` (única columna nueva; `mediciones` y `turnos` ya tenían tabla + RLS desde 001/002, solo faltaba UI). **Aplicada y verificada** (vía `psql`/pooler, mismo mecanismo que 001-003; confirmado el `ALTER TABLE`, `information_schema.columns`, y el schema cache de PostgREST reflejando la columna nueva vía introspección de `GET /rest/v1/` con la service role key).
  - Historia real de esto — dejarlo documentado porque el síntoma fue confuso: la migración se escribió pero no se aplicó de entrada. Eso rompió `/app/pacientes/[id]` con **404 en producción**, porque `page.tsx` seleccionaba `notas_generales` (que no existía todavía) y el código no distinguía "error de query" de "paciente no existe" — un select fallido se leía como `data: null` y caía directo en `notFound()`, sin loguear nada. Se corrigió el bug de código además de aplicar la migración: `page.tsx` ahora lee `error` del `select` y tira un error explícito (logueado) en vez de 404 mudo cuando la query falla — un 404 legítimo sigue siendo 404. Mismo criterio aplicado en `generarPlanIA` (`planes-actions.ts`), que tenía el mismo antipatrón. **Moraleja para el futuro**: una migración nueva en el repo no está aplicada hasta que este archivo diga explícitamente "aplicada y verificada" con la evidencia — no asumirlo por el commit ni por lo que diga el usuario sin confirmarlo primero.
- `src/app/app/pacientes/[id]/historia-clinica-panel.tsx` + `historia-actions.ts` — tres bloques: Mediciones (form para cargar peso + lista con delta vs. la medición anterior + sparkline SVG dibujado a mano, sin librería de gráficos), Historial de turnos (solo lectura — vacío hasta que exista Agenda, no hay CRUD de turnos todavía), Notas (textarea libre del profesional).
- **Trade-off de RLS documentado en la migración** (mismo patrón que `laboratorios.notas_profesional`): `notas_generales` es legible por el propio paciente a nivel de policy (`pacientes_select_self` es de fila completa, no filtra columnas) — la barrera es de capa de aplicación: ninguna query de `src/app/portal/**` selecciona esa columna. Si en algún momento hace falta la garantía a nivel de DB, la solución es una tabla aparte, no una policy de columna (Postgres no las tiene nativas).
- Mismo bug de key que `PlanIAPanel` evitado a propósito: el form de "agregar medición" usa `key={mediciones.length}` para resetearse solo cuando la lista realmente creció (mismo criterio, no un afterthought). El QA del agente `qa-tester` encontró que faltaba lo mismo un nivel más arriba: `<HistoriaClinicaPanel>` en `page.tsx` no tenía `key={paciente.id}` — sin eso, el `useState` de "notas" (inicializado solo en el mount) corría riesgo de mostrar/sobreescribir las notas de un paciente distinto si React llegaba a reconciliar la misma instancia entre dos fichas. Agregado.

**Chat** (RF-050/083, a partir del mockup `~/Documents/Pantallas/Chat/Chat1.png` — bastante más alcance que el RF-050 original de `docs/requirements.md`, que solo pedía "Mensajería 1:1... polling simple en v1"):
- `mensajes` (1:1) ya existía desde 001/002, sin UI. `supabase/migrations/011_chat_grupos_adjuntos.sql` suma lo que pedía el mockup y no estaba: **chat de grupo** (`chat_grupos` + `chat_grupo_miembros`, un grupo real donde los miembros se ven entre sí — es la semántica esperada de "grupo", no un bug de RLS) y **adjuntos** (`archivo_url`/`archivo_nombre`/`archivo_tipo` en `mensajes`, bucket privado `chat-adjuntos` sin whitelist de mime types, mismo límite de 15MB que `laboratorios`). `mensajes.paciente_id` pasa a nullable con un check (`(paciente_id is not null) <> (grupo_id is not null)`) — un mensaje es de un destino u otro, nunca los dos. `remitente_paciente_id` identifica qué paciente mandó un mensaje de grupo (en 1:1 ya lo dice `paciente_id`). Las 4 policies de `mensajes` de 002 quedan intactas — se sumaron policies nuevas para la rama de grupo (Postgres combina policies permisivas del mismo comando con OR, no hizo falta tocar las de 1:1). **Sin lectura por miembro en mensajes de grupo** (`leido` es un booleano por mensaje, no por destinatario — precisa una tabla de lecturas aparte que no se construyó en este pase, deliberado). **Aplicada y verificada** (`chat_grupos`/`chat_grupo_miembros` con 3 policies cada una, `mensajes` con 9 (6 originales de 002 + 3 nuevas de grupo), las 6 columnas nuevas confirmadas nullable, bucket `chat-adjuntos` privado con límite 15MB y 4 policies de `storage.objects`, todo vía `psql`/pooler).
  - **Bug real en la v1 de este archivo, corregido antes de aplicar**: la policy `chat_grupos_select_miembro` referenciaba `chat_grupo_miembros` antes de que esa tabla existiera en el mismo script — `psql` sin transacción explícita ya había commiteado `chat_grupos` + 1 policy cuando rompió ahí. Se relanzó una vez limpiado ese resto (`drop table chat_grupos cascade`) y con el archivo corregido (ambas tablas se crean primero, RLS y policies de las dos van después) **envuelto en `BEGIN`/`COMMIT` explícito** — si algo vuelve a fallar a mitad de camino, no queda nada a medio aplicar. Mismo patrón recomendado para migraciones futuras de más de una tabla nueva.
  - **`pre-commit-orchestrator` (workaround `general-purpose` con las 5 definiciones pegadas en el prompt — ninguna recargada en esta sesión)** corrió sobre el módulo entero: tsc/lint/build limpios, sin hallazgos bloqueantes de RLS (confirmó que las policies de grupo son aditivas, que el string-match exacto de `storage.foldername(name)[1]` no se puede bypasear, y que `remitente_paciente_id` no se puede spoofear vía FormData) y 2 hallazgos de seguimiento aplicados: **archivo huérfano en Storage** si el `insert` en `mensajes` falla después de subir el adjunto — `enviarMensaje`/`enviarMensajePaciente` ahora lo borran (best-effort) en ese caso; y **selects sin loguear `error`** en `obtenerConversaciones`/`obtenerConversacionesPaciente` — agregado.
- `src/lib/queries/chats.ts` (`obtenerConversaciones`) + `src/app/app/chats/actions.ts` — lista de conversaciones (1:1 + grupos) con último mensaje/adjunto y no-leídos, ordenada por actividad reciente ("status de cuándo se interactuó por última vez" del pedido, no presencia/online en vivo — decisión deliberada, ver abajo). `enviarMensaje` sube el adjunto a Storage antes de insertar (releyendo que el destino sea propio antes de subir, para no gastar la subida si el insert de todos modos va a fallar por RLS) y `crearGrupo` inserta el grupo + sus miembros en dos pasos (sin RPC atómica — no hay un cálculo cross-tabla que proteger más allá de lo que ya hace RLS, mismo criterio que `docs/architecture.md` para no-RPC).
- `src/app/app/chats/` — `chats-view.tsx` (dos paneles: lista con buscador por nombre + `NuevoGrupoDialog`, y la conversación activa), `message-bubble.tsx` (linkify de URLs en el texto, preview de imagen o chip de archivo para adjuntos, doble check de leído estilo WhatsApp), `query-provider.tsx` (TanStack Query — polling cada 4s en la conversación activa y cada 10s en la lista, primer uso real de la librería en el repo fuera de la dependencia declarada; **polling, no Realtime**, siguiendo la decisión ya documentada en `docs/architecture.md`). `NuevoGrupoDialog` usa un handler `async` manual en vez de `useActionState` + `<form action={fn}>` + `useEffect` — cerrar el diálogo y avisar al padre justo cuando la action resuelve, hecho desde un `useEffect` leyendo el estado, dispara el lint de React (`Calling setState synchronously within an effect`) porque reabrir el diálogo después reusaría el mismo estado "success" ya resuelto; con `await` directo en el handler el cierre pasa en el mismo evento que originó el submit.
- `src/app/portal/chat/` (RF-083) — mismo backend, UI más simple (sin buscador ni creación de grupos — el paciente no gestiona membresía). Reusa `MessageBubble` y `QueryProvider` de `src/app/app/chats/` (mismo patrón ya establecido de reusar componentes entre `/app` y `/portal`, ver `LoginForm` en `onboarding/invitacion/[token]/page.tsx`). Nav item nuevo en `PortalNav`.
- **Deliberadamente afuera de este pase** (no estaban en el pedido en texto del usuario, solo visibles como decoración en el mockup): la asignación "Chatbot"/"Atención humana" (auto-respuesta por IA — una feature real de agente, no de chat, con consideraciones de seguridad/responsabilidad médica que ameritan su propia conversación antes de construirla) y el rediseño completo de la sidebar que muestra el mockup (Biblioteca/Pagos/Catálogo/Página pública en vez de Alimentos/Cobros/Configuración — es un mockup de una versión más madura del producto, no algo que este pedido haya cubierto). "Status de última interacción" se interpretó como el timestamp del último mensaje por conversación (ya visible, ordena la lista), no presencia/online en vivo — eso necesitaría un canal de Realtime con heartbeat, fuera del alcance de "polling simple" que ya elegimos para todo lo demás del chat.

**Alta de paciente enriquecida** (RF-020, a partir de mockups nuevos del usuario en `~/Documents/pantallaNutri*.jpeg` — "Alta de paciente" con 3 vistas: "Lo cargo yo" / "Lo llena el paciente" / "En la ficha"):
- `supabase/migrations/008_perfil_paciente.sql` — `pacientes` gana campos opcionales (dni, obra_social, motivo_consulta, sede, quien_derivo, sexo_biologico) + tres bloques nuevos que llena el paciente desde el wizard (antecedentes de salud: condiciones/alergias/medicacion; hábitos y actividad: habitos_comidas/habitos_quien_cocina/habitos_movimiento; consentimiento_datos) + 5 columnas `*_completado_at` (una por sección, incluye datos_personales y contacto) que son la fuente de la completitud — no se infiere de la sola presencia de datos, porque el profesional puede precargar nombre/teléfono al crear sin que cuente como "sección completada por el paciente". Tabla nueva `invitacion_eventos` (log de "Actividad del link": enviado_whatsapp/abierto/seccion_completada). **Aplicada y verificada** (18/18 columnas nuevas confirmadas en `information_schema.columns`, `to_regclass('public.invitacion_eventos')` resuelve, vía `psql`/pooler).
- `supabase/migrations/009_rpc_perfil_paciente.sql` — dos RPCs nuevas, mismo criterio que `aceptar_invitacion` (SECURITY DEFINER + columnas whitelisteadas a mano, nunca update dinámico desde las claves del jsonb, para no reabrir la policy de update del paciente que 002 dejó deliberadamente afuera): `completar_seccion_perfil(seccion, datos)` (guarda una sección + marca `*_completado_at` + inserta el evento `seccion_completada`) y `registrar_evento_invitacion(token, tipo)` (log de `enviado_whatsapp`/`abierto`, bearer-auth por token porque "abierto" pasa antes de que el paciente tenga sesión — `enviado_whatsapp` sí valida que el profesional que llama sea el dueño de la invitación). **Aplicada y verificada** (`pg_proc` confirma las dos funciones).
- `supabase/migrations/010_aceptar_invitacion_idempotente.sql` — `aceptar_invitacion` pasa a ser idempotente: el mismo link se reabre más de una vez (wizard incompleto, o "Reenviar solo lo pendiente" desde la ficha) y antes el segundo request tiraba "ya tiene una cuenta vinculada" porque el update calificaba `where user_id is null`. Ahora, si la invitación ya estaba aceptada, es un no-op válido siempre que siga siendo la misma cuenta de Google — si no matchea, sigue siendo un error real. **Aplicada y verificada** (`pg_proc.prosrc` confirma la rama nueva).
- **Nota para el futuro**: `source .env.local` desde bash rompe — `RESEND_FROM_EMAIL` tiene el default `NutrIA <onboarding@resend.dev>` sin comillas, y bash lee `<`/`>` como redirección. Para leer una sola var desde ese archivo (ej. `SUPABASE_DB_PASSWORD` para `psql`), usar `grep '^VAR=' .env.local | cut -d= -f2-`, no `source`.
- `src/app/app/pacientes/nuevo/` — reemplaza al diálogo "Nuevo paciente" (`nuevo-paciente-dialog.tsx`, borrado) por una página completa con tabs "Lo cargo yo" (form real: nombre/email/teléfono + campos opcionales en chips progresivos, columna derecha con "Qué incluye el link" y preview "Le llega esto") y "Lo llena el paciente" (preview estático, no interactivo, del paso 1 del wizard — es una vista previa para el profesional, no un flujo funcional duplicado). **Deviation deliberada**: el mockup combina "Teléfono o email" en un campo único; se mantuvo el email separado y obligatorio porque `aceptar_invitacion()` matchea la invitación contra ese email exacto de la cuenta de Google — combinarlos rompería esa validación. La tercera vista del mockup ("En la ficha") no es un tab acá — se integró directo en la ficha real del paciente (ver abajo), porque muestra completitud de un paciente ya existente, no tiene sentido antes de crearlo.
- `src/app/onboarding/invitacion/[token]/perfil-wizard.tsx` + `wizard-actions.ts` — después de `aceptar_invitacion`, si quedan secciones con `*_completado_at` nulo, se muestra el wizard (Card standalone, mismo estilo que la pantalla de login) en vez de redirigir directo a `/portal`. Resume automáticamente en la primera sección pendiente y guarda paso a paso (no todo junto al final) — si el paciente cierra el link a mitad de camino, lo ya respondido queda guardado. Esto reemplaza la necesidad de un link con `?paso=X`: "Pedir" una sección puntual y "Reenviar solo lo pendiente" mandan el mismo link sin parámetros, total el wizard ya sabe dónde retomar.
- `src/app/app/pacientes/[id]/page.tsx` — nueva sección "Completitud del perfil" (antes de Historia clínica): % + checklist de las 5 secciones con fecha o botón "Pedir" (`pedir-seccion-button.tsx`, wa.me con el link de invitación, logueando `enviado_whatsapp` vía RPC en el click), panel "Pedir lo que falta" (chips de las pendientes + "Reenviar solo lo pendiente") y "Actividad del link" (últimos `invitacion_eventos`). El link absoluto se arma server-side con `headers()` (`host`), no con `window.location`, para no depender de un mount en cliente.
- Simplificación deliberada vs. el mockup: no se implementó el botón "Crear y enviar el link" con auto-apertura de WhatsApp al crear — `window.open()` disparado desde un `useEffect` después de que resuelve la Server Action casi siempre lo bloquea el popup blocker del navegador, porque para entonces ya no hay gesto de usuario síncrono. "Crear paciente" queda como una sola acción; el envío por WhatsApp siempre es un click explícito aparte (mismo patrón que ya usaba el diálogo viejo).
- **QA del agente (workaround `general-purpose` con la definición de `qa-tester.md` pegada en el prompt, la sesión no lo tenía recargado)** encontró y se corrigieron 3 hallazgos, ninguno bloqueante:
  - **Importante**: en `alta-paciente-view.tsx`, el input de `email` y los 5 inputs opcionales (dni/obra_social/motivo_consulta/sede/quien_derivo) estaban sin controlar dentro de `<form action={formAction}>` — mismo gotcha ya documentado de Agenda (React 19 resetea campos no controlados al terminar la transición, incluso en un resultado de error, y `crearPaciente` nunca lanza). Fix: todos controlados (`value`/`onChange`), mismo criterio que `nombre`/`telefono`.
  - Menor: la alerta de "sin registrar comidas" en el Inicio solo mostraba el paciente más atrasado sin indicar si había más — ahora suma `· +N más` cuando corresponde, igual que el resto de las alertas de la bandeja.
  - Menor: el fallback defensivo de `perfil-wizard.tsx` (`seccionesPendientes.length === 0`, inalcanzable hoy porque `page.tsx` ya redirige antes de montar el wizard) llamaba `router.replace()` directo en el cuerpo del componente — movido a `useEffect` para no disparar una navegación como side effect durante el render.

**Paleta y tipografía ya son las del diseño real** (no un placeholder): sidebar violeta/ciruela oscuro (`--sidebar`), fondo crema (`--background`), acento naranja/coral (`--accent`, `--destructive` para prioridad ALTA), heading serif con Google Font Lora (`--font-heading`, aplicado a h1/h2/h3 vía `@layer base`) — todo en `globals.css`. Los valores se estimaron a ojo de las capturas (no hay extractor de color exacto) — si algo se ve desalineado con el diseño original, es candidato a ajuste fino, no a rediseño.

**Pantallas construidas** (`src/app/app/`):
- `components/app-shell.tsx` — sidebar fijo con nav (Inicio/Pacientes/Agenda/Chats + sección Contenido: Planes/Alimentos/Recursos/Formularios + Configuración), estado activo por ruta, menú de usuario (dropdown) con logout.
- `/app` (Bandeja de hoy, RF-040/041) — alertas reales calculadas con queries, no hardcodeadas: pacientes activos sin próximo turno, turnos pendientes en las próximas 48h, cobros pendientes, laboratorios sin revisar hace más de 48h y (agregado después, ver `obtenerPacientesSinRegistrarComida` en `lib/queries/dashboard.ts`) pacientes dados de alta hace más de 7 días sin registrar comida en los últimos 7 — prioridad nueva `INFO` (sin dot/label, más sutil que ALTA/MEDIA) para esta última, matcheando el mockup. **Deliberadamente sigue sin mostrar** adherencia/recuperados agregados — se documentó como nota visible en el panel de métricas en vez de fabricar números. Métricas reales: % continuidad, $ por cobrar. Agenda de hoy con brief de continuidad (de `consultas`, solo si existe una fila real). Actividad reciente combina `mediciones`/`planes.enviado_at`/`turnos.created_at` (no incluye "turno confirmado" porque el schema no registra cuándo cambió el estado, solo cuándo se creó la fila).
- `/app/pacientes` (RF-020/021) — listado con búsqueda/filtros por query params (`?q=&estado=&sin_turno=`), banner de alerta reusando la misma query que la Bandeja de hoy (`lib/queries/pacientes.ts`), diálogo "Nuevo paciente" que llama a la RPC `invitar_paciente` y muestra el link de invitación para copiar + botón "Enviar por WhatsApp" (`wa.me`/`api.whatsapp.com`, con el teléfono cargado si hay o dejando elegir el contacto si no) — es el canal principal de invitación, no mail (ver más abajo por qué).
- `/app/agenda` (RF-030/032) — calendario (mes, `react-day-picker`) + lista de turnos del día seleccionado, traídos de una sola vez (últimos 30 días / próximos 120) y filtrados en el cliente al cambiar de día, sin ida y vuelta al server. Crear/editar turno (paciente, fecha, hora, tipo, notas — reasignar paciente no está en el alcance de "editar"), cambiar estado a mano (`pendiente/confirmado/en_curso/cancelado`, `<select>` nativo con auto-submit `onChange` — no el `Select` de shadcn/Base UI, para no lidiar con su timing async para un caso tan simple), y "Registrar brief" (RF-032, crea una fila en `consultas`). `/app/agenda?paciente=<id>` (ya linkeado desde el botón "Agendar" del listado de Pacientes) abre directo el diálogo de crear con ese paciente fijo.
  - **Fecha/hora — por qué se arma en el cliente**: el form manda un ISO ya calculado con `new Date(\`${fecha}T${hora}\`)` en el navegador del profesional, no `fecha`+`hora` sueltos para que el server los combine — Netlify corre las funciones en UTC, así que reconstruir la fecha en el servidor la interpretaría en UTC en vez de en la zona horaria real del profesional, corriendo el turno varias horas. Ver `turno-form-dialog.tsx` y el comentario en `agenda/actions.ts`.
  - **Integración con Google Calendar — decisión deliberada, no a medias**: se evaluaron dos alcances (ver la conversación que lo definió) y se eligió el más simple a propósito — `src/lib/calendar.ts` arma un link `calendar.google.com/calendar/render` con el evento precargado ("Agregar a Google Calendar" en cada turno), sin OAuth nuevo ni tokens guardados. **No sincroniza** — editar/cancelar un turno después no actualiza el evento ya agregado. El sync real de dos vías (crear/editar/borrar el evento solo) quedó evaluado y descartado para este alcance: requeriría pedir el scope `calendar.events` en el login (re-consentimiento de todos los profesionales ya logueados), guardar su refresh token, y muy probablemente una verificación de Google más estricta que la actual (`docs/architecture.md` §4 ya documenta la verificación de la app OAuth actual como el ítem de mayor lead time externo del roadmap — sumar el scope de Calendar la haría más estricta todavía).
  - **Bugs reales encontrados por el agente de QA en este módulo, corregidos** (ninguno lo agarraba build/lint):
    - `TurnoFormDialog` estaba siempre montado en `agenda-view.tsx` (solo alternaba la prop `open`) — el `useActionState` de adentro sobrevivía al cierre, así que crear dos turnos seguidos (o editar el mismo turno dos veces) sin pasar por otra identidad de por medio dejaba el diálogo pegado en la pantalla de "Turno creado/actualizado" de la vez anterior en vez de mostrar el form vacío. Fix: montarlo condicionalmente (`{(crearAbierto || turnoEditando) && <TurnoFormDialog .../>}`), mismo patrón que ya usaba `BriefDialog` al lado.
    - `pacienteFijo` (de `/app/agenda?paciente=<id>`) quedaba fijo para siempre después de la primera apertura — el botón genérico "Nuevo turno" no lo limpiaba, así que agendar a otro paciente después de entrar por ese link seguía mostrando el paciente viejo. Fix: `pacienteFijoActivo` como estado propio de `AgendaView` (no la prop directo), que el botón genérico resetea a `undefined` antes de abrir.
    - **React 19 resetea los campos no controlados de un `<form action={...}>` al terminar la transición de la action, incluso cuando el resultado es un error** — y las Server Actions de este proyecto nunca lanzan (`{status: "error"|"success"}`), así que ese reset nunca se puede esquivar devolviendo un error. Afectaba a tres campos que habían quedado no controlados por descuido (el resto del proyecto ya usa inputs controlados): el `<select>` de estado del turno (`estado-turno-select.tsx`, podía "saltar" a un valor viejo), y los campos de `registro-form.tsx`/`brief-dialog.tsx` (podían vaciarse justo al mostrar un error de validación). Los tres pasaron a controlados — no es un caso aislado, es un gotcha general de la convención `<form action={fn}>` + acciones que nunca lanzan: cualquier campo nuevo en ese patrón necesita `value`/`onChange`, no `defaultValue`.
- `/app/{planes,alimentos,recursos,formularios,configuracion,cobros}` — stubs "Próximamente" (`components/proximamente.tsx`) para que ningún link del nav ni de la Bandeja de hoy dé 404. `/app/chats` ya no es un stub — ver "Chat" más abajo.

**Invitación a paciente por WhatsApp, no por mail** (RF-020): se probó el envío automático por Resend y falló contra un paciente real — confirmado contra la API real (no solo compilado) que la cuenta de Resend está en modo sandbox y solo entrega al email dueño de la cuenta (`ijproyectos26@gmail.com`), a cualquier otro destinatario le devuelve 403 sin dominio verificado. Se sacó el envío de mail de `crearPaciente` (`pacientes/actions.ts`) y en su lugar `nuevo-paciente-dialog.tsx` tiene un botón "Enviar por WhatsApp" (`wa.me`/`api.whatsapp.com`) al lado del link para copiar. Si se verifica un dominio propio en Resend en el futuro, volver a esto es directo — el criterio "nunca lanza" de `src/lib/email/enviar.ts` sigue ahí.

**Email transaccional con Resend** (queda solo para RF-042, recordatorio manual de turno — `docs/architecture.md` §, sección "Email transaccional"):
- `src/lib/email/enviar.ts` — único punto de contacto con `resend` (`RESEND_API_KEY` en `.env.local`, `import "server-only"`). Mismo criterio "nunca lanza" que `src/lib/laboratorios/parsear.ts`: si el envío falla, devuelve `{ enviado: false, error }` en vez de tirar.
- `app/recordatorio-actions.ts` + `app/recordar-turno-button.tsx` — botón "Recordar a {paciente}" por turno sin confirmar, directo en la Bandeja de hoy (alerta de "turnos sin confirmar en las próximas 48h"). El turno se relee server-side por RLS antes de mandar el mail — no confía en lo que venga del form.
- **Mismo límite de sandbox que la invitación** (ver arriba): un recordatorio a un paciente real también va a fallar hasta que se verifique un dominio en Resend. Queda como está (no se pidió moverlo a WhatsApp) — si hace falta, mismo criterio que la invitación.

**Notas de lint que importan para código futuro en este repo**: eslint-config-next trae las reglas nuevas de React Compiler (`react-hooks/purity`, `react-hooks/set-state-in-effect`). No usar `Date.now()`/`Math.random()` en el cuerpo de un Server Component — usar `new Date()` (sí lo tolera, ver `src/lib/queries/*.ts` y `src/lib/format.ts` para el patrón). No hacer `setState` directo dentro de un `useEffect` — mover la lógica al handler que dispara el cambio (ver `nuevo-paciente-dialog.tsx`). No usar `window.location.href` para navegación interna — `useRouter()` de `next/navigation`. `<Button render={<Link .../>}>` (o cualquier `render` que no sea un `<button>` de verdad, ej. `<a>`/`Link`) necesita `nativeButton={false}` explícito — sin eso Base UI tira un console error en runtime ("expected a native <button>"), no se detecta en build/lint (bug real encontrado en `auth-code-error/page.tsx` y `onboarding/invitacion/[token]/page.tsx`, corregido). Cualquier campo dentro de un `<form action={fn}>` tiene que ser controlado (`value`/`onChange` o `checked`/`onCheckedChange`), no `defaultValue`/no controlado — React 19 resetea los campos no controlados de un form al terminar la transición de la action, sin importar si el resultado fue éxito o error, y como la convención de este repo es que las actions nunca lanzan (devuelven `{status: "error"}` en vez de tirar), ese reset no se puede esquivar. Bug real encontrado por el agente de QA en `estado-turno-select.tsx`/`registro-form.tsx`/`brief-dialog.tsx`, corregido — ver la nota en la sección de Agenda.

**Supabase está vivo**: proyecto ref `mgscprkbdbwejfhvkggm` (región `us-east-1`), URL `https://mgscprkbdbwejfhvkggm.supabase.co`. Las migraciones 001 a 010 están **aplicadas y verificadas** contra la DB en vivo (10 tablas base + `invitacion_eventos` de 008, RLS habilitado en todas, funciones `auth_profesional_id`, `auth_paciente_id`, `invitar_paciente`, `aceptar_invitacion`, `confirmar_turno`, `completar_seccion_perfil`, `registrar_evento_invitacion` — más el fix de `invitar_paciente` de 006, `pacientes.notas_generales` de 007, y el perfil de paciente enriquecido de 008-010). Aplicadas vía `psql` por el session pooler (`aws-0-us-east-1.pooler.supabase.com:5432`, user `postgres.mgscprkbdbwejfhvkggm`) — no hay Supabase CLI instalado, `psql` viene de `libpq` sin linkear al PATH (`find /opt/homebrew/Cellar/libpq -name psql` si cambia la versión). La contraseña del pooler está en `.env.local` (raíz, gitignored) como `SUPABASE_DB_PASSWORD` — a pedido explícito del usuario, aunque la app en sí no la lee (es solo para `psql`/migraciones). Si en algún momento no está o cambió, viene de Project Settings → Database → Connection string, o reseteándola ahí. No re-aplicar ninguna migración sin chequear el estado actual del proyecto primero.

**Trade-off de RLS sistémico, no nuevo de este pase** (notado al escribir `agenda/actions.ts`, pero ya estaba desde `mediciones`/`registros_comida`/`consultas`): las policies `*_insert_profesional` (`turnos_insert_profesional`, `mediciones_insert_profesional`, etc.) solo chequean `profesional_id = auth_profesional_id()` en el `with check` — ninguna valida que el `paciente_id` de la fila realmente pertenezca a ese profesional. Un Server Action que confíe en un `paciente_id` de `FormData` sin haberlo re-leído (a diferencia de `recordatorio-actions.ts`, que sí relee el turno) podría, en teoría, insertar una fila con `profesional_id` propio pero `paciente_id` de un paciente ajeno. Hoy no es explotable desde la UI (los `<select>` de paciente siempre listan solo los pacientes propios, ya filtrados por RLS de `pacientes`), pero es un endurecimiento pendiente si en algún momento se agrega un endpoint que acepte un `paciente_id` de una fuente menos controlada. Arreglo real si hace falta: `paciente_id in (select id from pacientes where profesional_id = auth_profesional_id())` en cada policy de insert, no parche por tabla.

Credenciales en `.env.local` (raíz, gitignored; `apps/web/.env.local` es symlink) — cargadas. Google provider activado en Supabase Auth y redirect URI agregado en Google Cloud Console (confirmado por el usuario, no verificable desde acá sin acceso a esos dashboards).

**Código de auth** (`npm run build`/`lint` limpios, y el usuario confirmó que el login con Google funciona en vivo — incluyó resolver un `redirect_uri_mismatch` corrigiendo el Authorized redirect URI en Google Cloud Console):
- `src/lib/supabase/{client,server}.ts` — clientes Supabase estándar SSR.
- `src/lib/dal.ts` — `getSession()`, `resolveRole()` (resuelve `profesional` / `paciente` / `sin_rol` en una sola query paralela, cacheada por request), `getAuthorizedProfesional()`/`getAuthorizedPaciente()` (guards de `/app` y `/portal`, redirigen cruzado si el rol no matchea en vez de rechazar — ver la nota en el archivo sobre por qué esto difiere del patrón allowlist de un proyecto de un solo tenant).
- `src/proxy.ts` — check optimista de cookie de sesión únicamente (sin query a rol), igual patrón que Raíces del Sur.
- `src/app/login/` — botón Google, forwardea `?next=` a través del callback.
- `src/app/auth/callback/route.ts` — solo intercambia el code, no decide rol (eso es `/` + `resolveRole()`).
- `src/app/onboarding/` — chooser (tabs) profesional/paciente; `actions.ts` tiene las dos Server Actions (`altaProfesional`, `aceptarInvitacionInput`); `invitacion/[token]/page.tsx` es el link directo que se le manda al paciente (auto-acepta si ya hay sesión).
- `src/app/portal/` — layout con la guard (`PortalNav`: Inicio/Mis turnos/Mi plan/Laboratorios/Registrar/Chat). RF-080 a RF-085 construidos: dashboard (próximo turno + confirmar, resumen del plan vigente, acceso a registrar), `/portal/turnos` (RF-082, listado propio + confirmar vía RPC `confirmar_turno`), `/portal/registro` (RF-081, form de comida/peso → `registros_comida`/`mediciones`), `/portal/plan` (RF-061), `/portal/laboratorios` (RF-084/085) y `/portal/chat` (RF-083, ver "Chat" más arriba). Nada del portal paciente queda como placeholder.

**Próximo paso real**: con Chat construido, los módulos centrales del PRD (Agenda, portal paciente completo, Chat) ya están. Sigue: Cobros (RF-070, hoy `lib/billing.ts` es un stub) y las pantallas de contenido (`alimentos`, `recursos`, `formularios`, `configuracion`) que siguen como "Próximamente".

## Qué es

NutrIA es un SaaS multi-tenant para nutricionistas en LATAM (self-serve: cualquiera se da de alta como profesional, sin allowlist) que centraliza pacientes, agenda, planes alimentarios, cobros y comunicación, con una "Bandeja de hoy" que prioriza alertas de continuidad. Dos frontales en el mismo Next.js app: panel profesional (`/app/*`) y portal paciente (`/portal/*`), separados por `proxy.ts` según a qué tabla pertenece el usuario logueado (`profesionales` vs `pacientes.user_id`).

Doc completo: `docs/product.md`. Requisitos funcionales (RF-xxx): `docs/requirements.md`. Esto fue armado a partir de un PRD/MVP entregado por el usuario (`docs/product-prd-original.md` y `docs/prompt-original.md`, conservados tal cual como referencia histórica) con una serie de recortes de alcance deliberados para v1 — están explicados con el porqué en `docs/product.md` y `docs/architecture.md` §6, no re-litigar sin revisar esa sección primero.

## Decisiones que difieren del PRD original (y por qué)

- **`profesional_id` denormalizado en toda tabla de negocio**, aunque también tenga `paciente_id` (`consultas`, `mediciones`, `registros_comida`) — el PRD original no lo tenía; se agregó para evitar policies RLS con subqueries anidadas.
- **`pacientes.user_id` en vez de una tabla de mapeo aparte** — el prompt original mencionaba una tabla `user_id -> paciente_id`; para MVP (1 paciente = 1 cuenta) alcanza una columna.
- **Alta de profesional self-serve, sin allowlist** — a diferencia de otros proyectos de un solo tenant, acá es el modelo de negocio: cualquiera se registra como profesional. El paciente sí sigue gateado por invitación (nunca se autorregistra).
- **Sin monorepo real** — el PRD lo sugería; no hace falta porque no hay backend propio, solo Supabase.
- Recortes de alcance para v1 (recordatorios manuales en vez de cron, planes en texto sin upload, sin CSV, sin auditoría) — confirmados con el usuario, detalle en `docs/product.md`.

## Stack

Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind CSS 4 + shadcn/ui (`base-nova`) + Lucide icons, sobre Supabase (Postgres, Auth, Row Level Security, Storage — bucket privado `laboratorios` en uso desde v1) + Anthropic API (`claude-opus-5`), hosted on Netlify (`netlify.toml`). Validación con Zod + React Hook Form. Server Components para lectura; TanStack Query solo donde haga falta cache/mutación del lado del cliente (ej. chat).

## Running the app

```bash
cd apps/web
npm run dev      # local dev, http://localhost:3000
npm run build    # production build
npm run lint     # eslint
```

**Next.js 16**: `middleware.ts` se llama `proxy.ts` (función exportada `proxy`). Antes de escribir código de App Router que no sea un tweak chico, revisar `apps/web/node_modules/next/dist/docs/` — el training data está desactualizado para lo que cambió Next 16.

## Docs map

- `docs/product.md` — visión, usuarios, alcance de v1 y qué queda fuera (con el porqué).
- `docs/requirements.md` — RF-xxx por módulo (auth, pacientes, agenda, dashboard, chat, planes, cobros, portal paciente).
- `docs/architecture.md` — stack, estructura de rutas, multi-tenancy/RLS, auth (self-serve profesional + invitación paciente), flujos críticos (RPCs), qué se difirió y por qué, infra recomendada.
- `docs/data-model.md` — entidades, campos, relaciones, reglas de integridad.
- `docs/product-prd-original.md` / `docs/prompt-original.md` — el PRD y el prompt tal cual los entregó el usuario, sin editar. Referencia histórica, no la fuente de verdad — para eso están los 4 docs de arriba.

## Supabase layout

- `supabase/migrations/001_initial_schema.sql`, `002_rls_policies.sql`, `003_rpc_funciones.sql` — escritas, **no aplicadas todavía** (no existe el proyecto Supabase). Aplicar en ese orden vía `psql` a través del session pooler cuando el proyecto exista (mismo mecanismo usado en Raíces del Sur — `db.<ref>.supabase.co` es IPv6-only e inalcanzable desde el sandbox, el pooler es el camino que funciona).
- `supabase/seed.sql` — no siembra `profesionales`/`pacientes` reales (requieren un `auth.users.id` real de un login real); tiene un ejemplo comentado para poblar pacientes de prueba una vez que exista un profesional real.
- Env vars (`.env.example`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (opcional, default `NutrIA <onboarding@resend.dev>`). Ninguna se expone al frontend — cargar las 6 en Netlify, no solo las 4 originales.
