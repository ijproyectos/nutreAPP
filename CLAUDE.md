# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

**Fase 6: todo lo de la Fase 5 (login en vivo, shell/dashboard/pacientes, laboratorios, plan con IA) + listo para deploy en Netlify.** `apps/web/` es Next.js 16 (Turbopack, App Router) — build y lint limpios. shadcn/ui en estilo `base-nova` (`@base-ui/react`, no Radix — composición con `render` prop, no `asChild`). `netlify.toml` en la raíz (mismo patrón monorepo de Raíces del Sur — `base = "apps/web"`, `publish = ".next"` explícito). Falta: crear el sitio en Netlify, conectar el repo de GitHub y cargar las 4 env vars ahí — eso es acción del usuario, no verificable desde acá.

**Laboratorios clínicos** (agregado post-v1, no estaba en el PRD original — ver `docs/architecture.md` §9 y `docs/requirements.md` RF-084/085/090/091 para el detalle):
- `supabase/migrations/004_laboratorios.sql` — tabla `laboratorios` + RLS + bucket privado de Storage (`laboratorios`) + policies de `storage.objects`. **Aplicada y verificada** (mismo mecanismo `psql`/pooler).
- `src/lib/laboratorios/parsear.ts` — extracción de texto de PDF con `unpdf` (sin OCR, sin dependencias nativas) + regex simples por analito. Nunca lanza — si falla, `valores = {}`.
- `src/app/portal/laboratorios/` — el paciente sube el archivo (Server Action `subirLaboratorio`: upload a Storage + insert + intento de parseo, todo en un solo paso; subió `experimental.serverActions.bodySizeLimit` a 15mb en `next.config.ts` porque el default de 1MB no alcanza para un PDF/foto) y ve su propio listado con estado.
- `src/app/app/pacientes/[id]/` — **ficha de paciente nueva** (no existía antes de esto — RF-022 solo estaba especificada, no construida). Tiene las secciones Historia clínica, Laboratorios (revisar/editar valores/validar/rechazar) y Plan alimentario (ver abajo).
- Bandeja de hoy: nueva alerta (`src/lib/queries/laboratorios.ts`) — laboratorios `pendiente_revision` hace más de 48hs.
- Trade-off de RLS documentado (no un bug): el paciente puede reescribir `notas_profesional` de su propio laboratorio mientras siga pendiente — ver la nota en `004_laboratorios.sql` y en `docs/architecture.md` §9 antes de "corregirlo" sin releer por qué se aceptó así.

**Generación de plan con IA** (`docs/architecture.md` §10, `docs/requirements.md` RF-062/063):
- `supabase/migrations/005_planes_ia.sql` — `planes` ganó `estado` (`borrador_ia`/`editado_manual`/`enviado`, default `editado_manual`), `generado_con_ia`, `laboratorio_id`. **Aplicada y verificada.**
- `src/lib/ai/generar-plan.ts` — único punto de contacto con la API de Anthropic (`@anthropic-ai/sdk`, `ANTHROPIC_API_KEY` en `.env.local`, `import "server-only"`). Modelo `claude-opus-5`. Usa `client.messages.parse()` con schema Zod (`output_config.format`) para forzar salida estructurada — nunca texto libre sin validar — y esa estructura se convierte a markdown antes de guardarse en `planes.contenido` (no hay columna jsonb paralela). **Verificado contra la API real** (no solo compilado): confirmé que el plan generado efectivamente ajusta según los valores de laboratorio pasados, no solo que la llamada no tira error.
- `src/app/app/pacientes/[id]/planes-actions.ts` + `plan-ia-panel.tsx` — "Generar plan con IA" (usa el laboratorio **validado** más reciente, nunca uno pendiente/rechazado) → `borrador_ia`; el profesional edita el texto libremente y "Guardar borrador" (`editado_manual`) o "Enviar al paciente" (`enviado`, recién ahí lo ve el paciente). Ningún camino de código salta directo a `enviado` sin pasar por esta pantalla.
- `src/app/portal/plan/page.tsx` — RF-061, el paciente ve su plan vigente (la policy RLS `planes_select_paciente` ya filtraba por `enviado_at is not null` desde antes de que existiera IA).
- Ya no hay gap: además de "Generar plan con IA", la ficha tiene "Escribir manualmente" (`crearPlanManual` en `planes-actions.ts`) — crea el plan directo en `editado_manual`, sin pasar por la IA (RF-060).
- Bug real corregido (reportado por el usuario: "genero el plan y no pasó nada"): `PlanIAPanel` es client component con `useState(planActivo?.contenido ?? "")` para el texto editable. Sin un `key` en el `<PlanIAPanel>` de `page.tsx`, React reusaba la misma instancia al pasar de "sin plan" a "con plan recién generado" — la acción sí insertaba el plan y `revalidatePath` sí traía el `planActivo` nuevo, pero el `useState` local quedaba pisado en `""` (su valor inicial, que solo se lee en el mount), así que el textarea se veía vacío y parecía que no había pasado nada. Fix: `key={planActivo?.id ?? "sin-plan"}` en `page.tsx` para forzar remount cuando cambia la identidad del plan.

**Historia clínica** (RF-022, completa la ficha de paciente — pedido explícito del usuario, antes solo estaban Laboratorios y Plan):
- `supabase/migrations/007_historia_clinica.sql` — agrega `pacientes.notas_generales` (única columna nueva; `mediciones` y `turnos` ya tenían tabla + RLS desde 001/002, solo faltaba UI). **Aplicada y verificada** (vía `psql`/pooler, mismo mecanismo que 001-003; confirmado el `ALTER TABLE`, `information_schema.columns`, y el schema cache de PostgREST reflejando la columna nueva vía introspección de `GET /rest/v1/` con la service role key).
  - Historia real de esto — dejarlo documentado porque el síntoma fue confuso: la migración se escribió pero no se aplicó de entrada. Eso rompió `/app/pacientes/[id]` con **404 en producción**, porque `page.tsx` seleccionaba `notas_generales` (que no existía todavía) y el código no distinguía "error de query" de "paciente no existe" — un select fallido se leía como `data: null` y caía directo en `notFound()`, sin loguear nada. Se corrigió el bug de código además de aplicar la migración: `page.tsx` ahora lee `error` del `select` y tira un error explícito (logueado) en vez de 404 mudo cuando la query falla — un 404 legítimo sigue siendo 404. Mismo criterio aplicado en `generarPlanIA` (`planes-actions.ts`), que tenía el mismo antipatrón. **Moraleja para el futuro**: una migración nueva en el repo no está aplicada hasta que este archivo diga explícitamente "aplicada y verificada" con la evidencia — no asumirlo por el commit ni por lo que diga el usuario sin confirmarlo primero.
- `src/app/app/pacientes/[id]/historia-clinica-panel.tsx` + `historia-actions.ts` — tres bloques: Mediciones (form para cargar peso + lista con delta vs. la medición anterior + sparkline SVG dibujado a mano, sin librería de gráficos), Historial de turnos (solo lectura — vacío hasta que exista Agenda, no hay CRUD de turnos todavía), Notas (textarea libre del profesional).
- **Trade-off de RLS documentado en la migración** (mismo patrón que `laboratorios.notas_profesional`): `notas_generales` es legible por el propio paciente a nivel de policy (`pacientes_select_self` es de fila completa, no filtra columnas) — la barrera es de capa de aplicación: ninguna query de `src/app/portal/**` selecciona esa columna. Si en algún momento hace falta la garantía a nivel de DB, la solución es una tabla aparte, no una policy de columna (Postgres no las tiene nativas).
- Mismo bug de key que `PlanIAPanel` evitado a propósito: el form de "agregar medición" usa `key={mediciones.length}` para resetearse solo cuando la lista realmente creció (mismo criterio, no un afterthought). El QA del agente `qa-tester` encontró que faltaba lo mismo un nivel más arriba: `<HistoriaClinicaPanel>` en `page.tsx` no tenía `key={paciente.id}` — sin eso, el `useState` de "notas" (inicializado solo en el mount) corría riesgo de mostrar/sobreescribir las notas de un paciente distinto si React llegaba a reconciliar la misma instancia entre dos fichas. Agregado.

**Rediseño visual definitivo** (completo — pedido explícito del usuario con referencia en `~/Documents/Pantallas/Nuevo/Rediseño visual de NutrIA Inicio/`, archivos `.dc.html` de Claude Design: `NutrIA Sistema de Diseño.dc.html` es la fuente de verdad del lenguaje visual — color por rol, tipografía, espaciado/radio, elevación, componentes con estados, estados de datos, movimiento — y `NutrIA Inicio/Pacientes/Ficha de Paciente/Agenda/Chats/Alta de Paciente/Planes.dc.html` son las pantallas mockeadas. Pendiente el pase del `pre-commit-orchestrator` sobre el conjunto — ver el bloque de hallazgos que se agrega acá cuando corra):
- **Base (fundamentos), aplicada a toda la app**: `globals.css` reescrito con los valores hex exactos del sistema de diseño (antes eran oklch estimados "a ojo") — ciruela de marca `#5C3A5E`, sidebar `#3D2740`, fondo `#F8F5F7`, alerta media `#C4792F`/tinte `#FBEEE0` (token nuevo `--warning`, separado de `--accent` porque ese ya se usaba en ~30 lugares como tinte de marca), positivo `#9CAF88`/tinte `#EDF1E7` (token nuevo `--positive`), destructive remapeado a `#9E4436` (la versión legible como texto, no el `#B4483A` "pleno" que el sistema reserva para puntos/rieles). Tipografía: Newsreader reemplaza a Lora (voz de marca, poco uso — saludo, títulos de página/sección, una cifra por tarjeta) e Instrument Sans reemplaza a Geist (toda la interfaz operable). De paso, corregido un bug real preexistente en `--font-sans: var(--font-sans)` (autorreferencia circular, CSS inválido — la utilidad `font-sans` caía al stack default de Tailwind en vez de la tipografía real).
- **`app-shell.tsx` (sidebar, compartido por todo `/app/*`)** reconstruido exacto al mockup — cambio estructural real, no solo de color: **el nav principal pasa a ser solo texto, sin íconos** (así está en las 3 pantallas del rediseño y en el componente "Ítem de navegación" del sistema de diseño). Se sumó **"Cobros" al nav** (gap real: el módulo existe desde antes y nunca había tenido entrada en el sidebar) y **"Links" queda deshabilitado** en gris (el panel de links públicos/reserva online del mockup es una feature nueva que no existe en el sitio — no se fabricó, se dejó visible pero sin acción, mismo criterio que "Compartir" en Inicio). El punto de aviso en "Agenda" ahora es real: `app/app/layout.tsx` calcula turnos sin confirmar (misma query que la Bandeja de hoy) y lo pasa como prop.
- **Inicio (`/app/page.tsx`), fidelidad completa al mockup, con datos reales** (no una maqueta): filas de la Bandeja de hoy con tratamiento visual distinto por prioridad (ALTA con riel+tinte+CTA en pastilla sólida, MEDIA con ícono+link, INFO discreta) en vez del layout uniforme anterior. **Agenda de hoy pasó a ser expandible** (`agenda-de-hoy.tsx`, client component) — antes solo el turno "en curso" mostraba su brief de continuidad destapado; ahora cualquier turno se puede abrir, así que `obtenerAgendaDeHoy()` (`lib/queries/dashboard.ts`) se reescribió para traer el brief de TODOS los turnos del día, no solo el en curso (y ganó `pacienteId`, que no traía). **Mini calendario del mes agregado de verdad** (`mini-calendario.tsx` + `lib/queries/calendario.ts`, `obtenerCalendarioMes`) — gap ya señalado desde el primer mockup de Alta de paciente ("no existe"), ahora resuelto con navegación de mes real por query param (`?mes=YYYY-MM`) y puntos reales (con turnos / sin confirmar) calculados de `turnos`, sin librería de calendario. El panel "Esta semana" (continuidad/por cobrar) se mantuvo aunque el mockup de Inicio no lo muestra — es dato de negocio real ya construido, sacarlo sería una regresión funcional disfrazada de limpieza visual, no algo que pidiera el rediseño.
- **Botón primario/secundario (`button.tsx`)** reescrito para los estados exactos del sistema de diseño — radio 10px explícito (no la escala `--radius-lg` heredada), y el hover del botón secundario pasó a cambiar solo el borde + una sombra chica (nunca el fondo, que recién cambia en `:active`) — antes el hover sí cambiaba el fondo, no coincidía con la spec.
- **Pacientes (`/app/pacientes/page.tsx`)**: buscador plano (no el `Input` de shadcn) matcheando dimensiones exactas del mockup, banner cálido con los hex reales (`--accent` dejó de significar "tinte cálido" cuando se separó `--warning`), filas de pacientes sin próximo turno con tinte + riel inset (`shadow-[inset_3px_0_0_#B4483A]`).
- **Ficha de paciente (`/app/pacientes/[id]/page.tsx`)** — primer pase (fase 1 del rediseño): pasa de 4 secciones apiladas a un tab bar (`ficha-tabs.tsx`), conservando los nombres/contenido reales (Historia clínica/Laboratorios/Plan alimentario/Completitud del perfil). Header restyleado (breadcrumb, título Newsreader, línea de subtítulo con edad/contacto/próximo turno/archivado). **Deep-link real nuevo** botón "Chat" → `/app/chats?paciente=<id>` (`chats/page.tsx`/`chats-view.tsx` ganaron `destinoInicial`).
- **Ficha de paciente — segunda fase, las 5 pestañas completas** (pedido explícito del usuario después de ver un segundo mockup más detallado de esta misma pantalla, en `~/Documents/Pantallas/Cobros y Link/`: en la primera fase se había decidido NO adoptar los 4 tabs del mockup por implicar funcionalidad no construida; el usuario pidió explícitamente construirlos completos igual, confirmado vía pregunta). Las 4 pestañas del mockup (Consulta/Historia/Archivos/Datos) más **Plan alimentario**, que el mockup no tiene como tab propio (asume que el manejo de planes vive en el módulo "Planes" de la sidebar, todavía no construido) — sacar el generador de planes con IA de la ficha sin tener dónde ponerlo sería una regresión real, así que se mantiene como pestaña aparte. Estado activo por query param (`?tab=`), no en memoria — mismo criterio que el resto del rediseño, permite linkear directo a una pestaña (ej. "Ver plan" desde Consulta).
  - **Consulta** (`consulta-panel.tsx`, pestaña por defecto): "De qué veníamos" (último `acordado` anterior a hoy) + "Qué pasó desde entonces" (días con registro de comida / delta de peso del mes — **sin las líneas de "hambre alta"/"último acceso al plan" del mockup**, no hay ningún dato real detrás de ninguna de las dos) + carga de medición y gráfico de peso (SVG propio, `peso-chart.tsx`, reemplaza al viejo `Sparkline`) + "Notas de hoy" con **autoguardado real** (debounce de 1.2s, `guardarNotaHoy` en `consulta-actions.ts`, escribe sobre la fila de `consultas` de HOY — la misma que "Registrar brief" de Agenda pudo haber creado, no una paralela) + **"Enviar como devolución" real**: no hay generación de PDF en el proyecto (ver Configuración → Plantillas de certificados), así que la devolución es un mensaje de Chat común (`enviarDevolucion`, inserta en `mensajes`) — el paciente la ve donde ya mira todo lo demás. Panel lateral: Plan vigente (link a `?tab=plan`), Adherencia (grilla de 21 días con el booleano real `registros_comida.adherencia`), Pendiente de cobro + "Enviar recordatorio" (link `wa.me`, mismo patrón manual que el resto de la app — no hay recordatorio de cobro automático).
  - **Historia** (`historia-timeline-panel.tsx`): reemplaza los 3 bloques que tenía antes (Mediciones/Turnos/Notas — mediciones se mudó a Consulta, notas privadas a Datos) por **una línea de tiempo única y filtrable** con datos 100% reales: `consultas`, `mediciones`, `planes` (creado/enviado), `invitacion_eventos` (formulario completado por sección) y el alta del paciente, mezclados y ordenados en `page.tsx`. Chips Todo/Consultas/Mediciones/Planes/Formularios — **sin "Devoluciones"**, a diferencia del mockup: una devolución acá es un mensaje de Chat común, no un registro distinguible de cualquier otro mensaje.
  - **Archivos** (`archivos-panel.tsx`): agrupa Laboratorios (sin tocar, mismo flujo de revisión de siempre) + **Documentos, tabla y bucket nuevos** (`documentos_paciente`/`documentos-paciente`, migración 018 — genéricos, para informes/fotos que no sean un laboratorio; **solo el profesional sube en esta v1**, no hay pantalla equivalente en el portal paciente) + Planes enviados (lista que linkea a `?tab=plan`). **Deliberadamente NO incluye "Informes de consulta" como PDFs auto-generados** — no hay infra de generación de PDF en el proyecto.
  - **Datos** (`datos-editables-panel.tsx`, reemplaza a "Completitud del perfil"): mismo % y barra segmentada de antes, pero ahora con **las 4 secciones de datos editables directamente por el profesional** (Contacto/Personales/Antecedentes/Hábitos, `editar-seccion-dialog.tsx` + `actualizarSeccionPerfil` en `datos-actions.ts` — `pacientes_update_profesional` de 002 ya permite esto por RLS, no hizo falta RPC ni migración nueva, solo columnas whitelisteadas a mano por sección igual que la RPC del paciente). **"Consentimiento" queda deliberadamente afuera** de lo editable — no es un dato que el profesional deba poder marcar en nombre del paciente. Dos campos que muestra el mockup no tienen columna real en este proyecto y se omitieron sin fabricarlas: "Ocupación"/"Altura" (sección Personales) y "Sueño" (Hábitos) — agregarlas tocaría también el wizard del paciente, fuera de alcance de este pase. "Pedir lo que falta"/"Actividad del link" se mudaron acá desde la vieja Completitud, sin cambios. "Notas privadas" (antes en Historia) también vive acá ahora — nunca visible para el paciente, mismo trade-off de RLS ya documentado.
  - `supabase/migrations/018_ficha_documentos.sql` — tabla `documentos_paciente` + bucket privado `documentos-paciente` (15MB, sin whitelist de mime types, mismo patrón de path que `chat-adjuntos`). **Aplicada y verificada** (tabla con su policy `for all`, bucket privado confirmado, 2 policies de `storage.objects`, todo vía `psql`/pooler).
- **Chats (`/app/chats/*`)**: lista rediseñada con buscador plano, banner "orden por atención, no por fecha" — **y ahora es cierto**: la lista pasó a ordenar de verdad por prioridad (pacientes sin próximo turno primero, estable sobre el orden por actividad que ya traía `obtenerConversaciones`), antes solo mostraba el texto sin cambiar el orden real. Fila con riel rojo + badge "Sin próximo turno" / badge verde "Turno D mmm" (`proximoTurnoPorPaciente`, calculado en `chats/page.tsx`), mismos badges en el header de la conversación activa + link "Agendar →". Quick actions reales en el footer del chat 1:1: "Agendar turno" (`/app/agenda?paciente=`) y **"Cobrar" nuevo** (`/app/cobros?paciente=`, ver abajo) — se omitió "Adjuntar plan" del mockup porque no hay flujo real de adjuntar un plan ya existente (el adjunto genérico por clip ya cubre mandar un archivo). `message-bubble.tsx` con el radio asimétrico del sistema de diseño (5px en la esquina "de origen") y timestamp fuera de la burbuja.
  - **Cobros ganó `?paciente=<id>`** (`cobros/page.tsx` + `nuevo-cobro-dialog.tsx`) para que el deep-link desde Chats abra el diálogo con el paciente ya elegido — estado inicial con **init perezoso** (`useState(() => ...)`), no `useEffect` con `setState` síncrono al principio del cuerpo (dispara `react-hooks/set-state-in-effect` — hasta llamar a una función que a su vez hace `setState` síncrono lo dispara, el lint sigue la referencia). El fetch de las consultas del paciente resuelve en un `.then()`, no en el cuerpo del efecto.
- **Agenda (`/app/agenda/*`), solo vista Día** (ver más abajo por qué no Semana): header con título Newsreader del día seleccionado + resumen (turnos/sin confirmar), 2 tarjetas de señal arriba del calendario — "Alta" (pacientes activos sin próximo turno, `obtenerPacientesSinProximoTurno`) y "Media" (turnos sin confirmar en 48h, `obtenerTurnosSinConfirmar` — el mockup dice "esta semana", se mantuvo la ventana de 48h ya establecida en la Bandeja de hoy para no definir un segundo criterio de "próximo" solo acá), ambas con acción real. `turno-row.tsx`: riel de color por estado (verde confirmado/en curso, ocre pendiente), badge "Sin confirmar"/"Confirmado", link nuevo "Abrir ficha" (`/app/pacientes/[id]`). "Modalidad" en el diálogo de turno pasa de `<select>` a pills (presencial/videollamada).
  - **Deliberadamente NO construido**: la vista "Semana" del mockup (grid franja×día — feature nueva, no solo visual, hoy no existe ni el cálculo de franjas ni la UI), el buscador de paciente con avatar en el modal (el `<select>` ya cumple lo mismo), "Duración" y "Tipo de consulta" como campos nuevos (no hay columna para ninguno — agregarlos fabricaría alcance) y el toggle "Enviar recordatorio" (los recordatorios ya son manuales vía botón dedicado, no hay nada que un switch en el modal controle de verdad).
- **Alta de paciente, las 3 vistas del mockup**: "Lo cargo yo"/"Lo llena el paciente" (`alta-paciente-view.tsx`, switcher segmentado, footer bar propio con "Enter para crear", panel oscuro `#3D2740` "Le llega esto" ahora también post-creación con el link real + WhatsApp/Copiar) y "En la ficha" = tab "Completitud del perfil" de la Ficha de paciente (ver arriba, no un tab acá — sigue siendo donde tiene sentido, en el paciente ya creado). El wizard real que ve el paciente (`onboarding/invitacion/[token]/perfil-wizard.tsx`) ganó header propio separado del cuerpo de la pregunta (Newsreader), pills de sexo biológico restyleados, y dos piezas de funcionalidad real nuevas que el mockup mostraba y no existían: **botón "Atrás"** (`setPasoIdx - 1`, no había forma de retroceder) y la nota **"Podés cerrar y seguir después"** (cierta: el guardado ya es por sección). **Deliberadamente NO construido**: los selectores por chips de condiciones/alergias/medicación/cocina/deportes (hoy son columnas de texto libre, no arrays — pasar a tags de una taxonomía fija es cambio de schema, no de estilo), el contador +/- de "comidas por día", "¿por dónde preferís que te escriba?" (mismo motivo: no hay campo real detrás de ninguno).
- **Pase de consistencia en pantallas sin mockup propio** (Cobros, Suscripciones, Configuración×17, Portal paciente, login, invitación, auth-code-error): estas heredan la base (colores/tipografía/radios de los primitivos compartidos), no se re-maquetaron pantalla por pantalla, pero se corrigió un bug visual real que el swap de tipografía había dejado — `h1` ya hereda Newsreader por la regla nueva de `globals.css`, pero varias pantallas seguían con la clase vieja `"text-2xl font-bold text-primary"` pensada para Geist, y el resultado era un serif en negrita 700 que el sistema de diseño no usa en ningún lado (Newsreader va en 400/500/600) — cambiado a `"font-heading text-2xl"` (o `text-xl`) en las ~10 pantallas afectadas. También: colores de éxito hardcodeados (`emerald-100`/`emerald-600`) migrados a los tokens `--positive`/`--positive-foreground` (laboratorios, suscripciones, historia clínica, registro de comida), y 3 usos de emoji como ícono (🥗 login/invitación, ⚠️ las dos pantallas de error) reemplazados — el sistema de diseño lo pide explícito ("sin emojis, sin caracteres tipográficos como íconos"): el logo pasa al mismo lockup "N" con gradiente de la sidebar, el warning a `TriangleAlert` de lucide. Se dejó el 👋 del saludo del portal paciente — es calidez textual, no un ícono de UI.
- **Planes** sigue sin tocar — mockup de una feature que todavía no está construida (`/app/planes` sigue "Próximamente"); se toca cuando se construya Planes, no antes.
- **Segundo lote de mockups** (`~/Documents/Pantallas/Cobros y Link/Rediseño visual de NutrIA_cobros/`, mismo sistema de diseño — `NutrIA Sistema de Diseño.dc.html` es idéntico byte a byte al del primer lote): los 7 archivos que se repiten (Inicio/Pacientes/Chats/Alta de Paciente/Agenda/Ficha de Paciente/Planes) difieren del lote anterior en una sola línea cada uno — el link de "Cobros" del sidebar, que pasa de `href="#"` a apuntar al archivo nuevo — confirmado con un diff, no a ojo. Ninguno se volvió a tocar por eso. El único archivo realmente nuevo es `NutrIA Cobros.dc.html`, ver la sección siguiente.

**Cobros — catálogo de precios (Servicios/Productos)** (a partir de `NutrIA Cobros.dc.html`, el mockup nuevo del segundo lote — bastante más que un reskin visual del `/app/cobros` que ya existía: un catálogo de precios completo, con un aumento masivo y un tercer tab que recién ahí es el `/app/cobros` de siempre. Confirmado con el usuario antes de construir, vía pregunta explícita: build completo, no solo la pestaña de pagos):
- `supabase/migrations/017_catalogo_precios.sql` — tabla nueva `servicios_precios` (profesional_id, nombre, `clase` consulta/paquete/producto, `duracion_o_entrega` texto libre relabeleado según la clase, `modalidad` presencial_video/videollamada/domicilio/digital, precio, `publico` — "en tu link de reservas" —, `archivado`, `precio_actualizado_at`), policy `for all` de siempre. `cobros` gana `servicio_id` opcional (nullable, sin `on delete`/`cascade` — nunca hay un delete real de `servicios_precios`, solo archivado, ver abajo). RPC `aplicar_aumento_precios(pct, alcance)` SECURITY INVOKER: el precio nuevo de cada fila depende de su propio precio actual (`round(precio*(1+pct/100)/500)*500`), eso es una expresión de columna que el cliente de Supabase no puede mandar en un `.update()` con valores literales — necesitaba resolverse en una sola sentencia SQL atómica en el servidor, mismo criterio que la RPC de suscripciones (016, dinero de por medio). **Aplicada y verificada** (`servicios_precios` con sus 3 check constraints y la policy `servicios_precios_all_self`, `cobros.servicio_id` confirmada, `aplicar_aumento_precios` con `prosecdef = f` — SECURITY INVOKER — todo vía `psql`/pooler, migración completa envuelta en `BEGIN`/`COMMIT`).
- **"Archivar", nunca "Eliminar"** (soft delete, `archivado boolean`): un cobro ya generado con `servicio_id` apuntando a un ítem archivado tiene que seguir pudiendo resolver su nombre/precio histórico — por eso no hay ningún camino de borrado real en el código, ni falta hacerle `on delete set null` al FK.
- **"Mostrar en tu link de reservas" (`publico`) se construyó igual, sin consumidor todavía** — a diferencia del criterio "solo con consumidor real" que rigió Configuración, acá el usuario lo pidió explícito sabiendo que el link de reservas público no existe (`Configuración → Reservas online` sigue "Próximamente"). Cuando se construya esa feature, la columna ya está lista — no hace falta ninguna migración nueva para leerla.
- **"Actualizar precios" (aumento masivo) sin envío automático por WhatsApp** — el mockup lo muestra como un toggle "avisar a los pacientes activos" que sugiere una notificación en bloque; esta app no tiene (ni tuvo nunca) un canal de envío automático, todo WhatsApp acá es un link `wa.me` que abre el propio profesional. El toggle se mantuvo porque tiene un comportamiento real detrás: si queda activado, después de aplicar el aumento aparece una lista de pacientes activos con un link de WhatsApp prearmado por cada uno (`aumento-precios-dialog.tsx`) — el profesional los manda uno por uno, como el resto de la app (invitaciones, recordatorios, aumento de precios). No es una automatización fabricada, es el mismo patrón manual de siempre.
- **"Uso este mes" / ingresos por servicio son reales, no inventados**: solo empiezan a poblarse desde que existe `cobros.servicio_id` — `NuevoCobroDialog` ganó un `<select>` opcional de servicio que autocompleta el monto (igual editable) y guarda el link; sin elegir uno, el cobro sigue siendo 100% manual como siempre. Los cobros viejos (todos, antes de esta migración) quedan con `servicio_id` nulo — el catálogo arranca en 0 usos, no se fabricó un backfill inventando a qué servicio correspondía cada cobro histórico.
- **Estado "Vencido" es de presentación, no una columna nueva**: `cobros.estado` sigue siendo solo `pendiente`/`cobrado` (no se tocó el enum ni ninguna policy) — "vencido" se calcula en `lib/queries/cobros.ts` (`pendiente` + `fecha_vencimiento` pasada) y solo cambia cómo se pinta la fila.
- **Rutas reales, no tabs en memoria** (mismo criterio que toda la app desde el rediseño): el mockup tiene 3 pestañas con estado de componente (Servicios/Productos/Pagos); acá son `/app/cobros/servicios`, `/app/cobros/productos` y `/app/cobros` (Pagos, la URL de siempre — no se movió para no romper los deep-links ya existentes `/app/cobros?paciente=<id>` desde Chats/Ficha). Se sumó una 4ª pestaña real, "Suscripciones", que el mockup no contempla porque es anterior a este mockup — `CobrosTabs` pasó de 2 a 4 pestañas. El link "Cobros" del sidebar (`app-shell.tsx`) se dejó apuntando a `/app/cobros` (Pagos), no a Servicios — es la vista más usada hasta ahora y la que ya tenía deep-links reales; deviation deliberada del `pestanaInicial` default del mockup.
- **El editor de un ítem no deja elegir "Qué es" libremente** (a diferencia del mockup, que lo permite en cualquier pestaña y decide a qué lista va según lo elegido): en `/app/cobros/servicios` el picker ofrece Consulta/Paquete; en `/app/cobros/productos` la clase queda fija en Producto, sin picker. Evita la ambigüedad de "creaste un producto estando en la pestaña de servicios, ¿a qué ruta te mandamos?" que las rutas reales (vs. tabs en memoria) introducen — el mockup no tiene ese problema porque todo vive en un solo estado de componente.
- `components/ui/toggle-switch.tsx` — primer interruptor on/off real del proyecto (no hay Switch de shadcn instalado; hasta ahora toda preferencia booleana usaba Checkbox). Usado acá (público / avisar) — reusable para lo que siga.

**Suscripción y facturación (pedido explícito del usuario, sin RF asignado — dos direcciones distintas que confirmó querer las dos):
- **Paciente → Profesional** (`/app/cobros/suscripciones`, nuevo tab al lado de `/app/cobros` vía `cobros-tabs.tsx`): cobro recurrente a pacientes, extiende Cobros (RF-070/071) en vez de reemplazarlo — una suscripción no cobra sola, genera filas normales de `cobros` (con `suscripcion_id` opcional, columna nueva) que se marcan "cobrado" igual que siempre. **Confirmado con el usuario: seguimiento manual, sin pasarela de pago** (mismo criterio que ya rige Cobros; `lib/billing.ts` sigue siendo el único stub de cobro online). `supabase/migrations/015_suscripciones_pacientes.sql` — `planes_suscripcion` (catálogo del profesional: nombre/monto/frecuencia) + `suscripciones_pacientes` (paciente↔plan, `estado` activa/pausada/cancelada, `proximo_vencimiento` — se **avanza a mano** cada vez que se genera un cobro, no se deriva de fecha_inicio+frecuencia en cada query porque eso se rompe con pausar/reactivar).
  - **Sin cron, a propósito** (mismo criterio ya documentado — "recordatorios manuales en vez de cron"): `generarCobroSuscripcion` es un botón explícito que aparece cuando `proximo_vencimiento <= hoy` (`vencida`, calculado en `lib/queries/suscripciones.ts`, no guardado) — nada se genera solo. Alerta nueva en la Bandeja de hoy (`contarSuscripcionesVencidas`) para que no haya que entrar a mirar la pantalla para enterarse.
  - `eliminarPlanSuscripcion` — el FK de `plan_id` en `suscripciones_pacientes` es sin `cascade`/`set null` a propósito (borrar un plan con suscripciones activas tiene que fallar explícito). El código detecta el código `23503` (violación de FK) y devuelve un mensaje específico ("Hay pacientes suscriptos a este plan…") en vez del genérico.
- **Profesional → NutrIA** (`/app/configuracion/suscripcion`): **deliberadamente mínima** — muestra el plan real (`profesionales.plan_id`, existe desde 001, default `'free'`), sin tabla de precios ni tiers inventados. No hay ningún plan pago, feature-gating ni integración de cobro definidos en ningún doc del proyecto — construir eso sería fabricar una decisión de negocio, no un detalle técnico, a diferencia del resto de Configuración donde "solo con consumidor real" alcanzaba con no construir un toggle decorativo. Esto sigue necesitando la conversación de producto que ya se había señalado.
- **Migración 015 aplicada y verificada** (`planes_suscripcion`/`suscripciones_pacientes` existen con su policy `for all` cada una, `cobros.suscripcion_id` confirmada nullable, vía `psql`/pooler).
- **`pre-commit-orchestrator` encontró 1 hallazgo bloqueante real, con dinero de por medio** — `generarCobroSuscripcion` hacía dos escrituras sueltas desde el cliente (insert en `cobros`, después update de `proximo_vencimiento`) sin transacción. Dos problemas confirmados, no solo teóricos: (1) si el update fallaba después del insert, `proximo_vencimiento` nunca avanzaba y la suscripción quedaba "vencida" para siempre — un reintento generaba un SEGUNDO cobro para el mismo período, sin ningún constraint que lo impidiera; (2) race condition real entre dos requests concurrentes (doble click, dos tabs) que podían leer la suscripción antes de que cualquiera escribiera. **`supabase/migrations/016_generar_cobro_suscripcion_rpc.sql`** — fix con dos capas: `unique index` en `cobros(suscripcion_id, fecha_vencimiento) where suscripcion_id is not null` (ningún camino de código puede duplicar un cobro del mismo período) + RPC `generar_cobro_suscripcion` (SECURITY INVOKER, insert+update atómicos con `select ... for update` — la segunda de dos llamadas concurrentes espera a que la primera commitee y ya no pasa la validación de "vencida"). La aritmética de fecha para "mensual" pasó a SQL con clamp de fin de mes (`least()` entre "mismo día del mes siguiente" y "último día del mes siguiente") — la versión en TS (`avanzarFecha`, ya borrada) tenía el mismo bug que el agente confirmó corriéndolo: `31 ene + 1 mes` daba `3 mar` en vez de `28/29 feb` (`Date.UTC` + `setUTCMonth` desborda igual que Postgres `+ interval '1 month'` sin clamp). **Aplicada y verificada** (`idx_cobros_suscripcion_periodo` existe, `generar_cobro_suscripcion` creada con `prosecdef = f` — SECURITY INVOKER, como corresponde).
  - **Importante, también aplicado**: `crearSuscripcion` ahora bloquea una segunda suscripción activa del mismo paciente al mismo plan — antes dos clicks accidentales creaban dos filas independientes, cada una con su propio "Generar cobro" (cobro recurrente duplicado real). Un paciente en dos planes *distintos* sigue siendo válido, no se bloquea.

**Configuración — 11 secciones más construidas** (sin mockup nuevo — el usuario pidió terminar "los componentes pendientes" con criterio propio, dado que no hay más capturas que la de "Cuenta"; confirmado con el usuario antes de arrancar). Mismo criterio en las 11: **solo se construyó lo que tiene un consumidor real ya en el código** — nada de toggles decorativos que no cambian ningún comportamiento (mismo espíritu que la tarjeta "Seguridad" de Cuenta). `supabase/migrations/013_configuracion_consultorio.sql`:
- **`sedes`/`obras_sociales`** — catálogos CRUD propios del profesional (tabla nueva cada uno, políticas `for all` scoped por `profesional_id`). Standalone en este pase: NO están linkeados a `pacientes.sede`/`pacientes.obra_social` (siguen siendo texto libre ahí, de 008) — es un follow-up explícito, no algo que se haya intentado y quedó a medias.
- **Grupos de pacientes** — no es una feature nueva: lee `chat_grupos`/`chat_grupo_miembros` (ya existentes del módulo Chat) y muestra un resumen de solo lectura con conteo de miembros + link a `/app/chats`, que sigue siendo donde se gestionan de verdad.
- **Mi Google Calendar** — informativa, sin schema: explica la integración simple que ya existe (link "Agregar a Google Calendar" por turno) y por qué la integración más profunda se descartó a propósito — no una pantalla de "conectar/desconectar" que sugeriría algo que no existe.
- **Mi agenda** — `profesionales.tipo_turno_default` (presencial/videollamada), wireado de verdad: `agenda/page.tsx` lo lee y lo pasa a través de `AgendaView` → `TurnoFormDialog`, que lo usa como valor inicial del `<select>` de tipo al crear un turno (no pisa el tipo ya cargado al editar). **No se agregó "duración por defecto"**: `turnos` no tiene columna de duración (solo `fecha_hora` puntual), así que ese campo no habría tenido nada real que precargar.
- **Comunicación** — `plantilla_invitacion_whatsapp` (placeholders `{nombre}`/`{profesional}`) y `plantilla_recordatorio_email` (`{nombre}`/`{fecha}`). La primera reemplaza el texto hardcodeado en `alta-paciente-view.tsx` (`textoInvitacion()`, usado tanto por el botón de WhatsApp como por el preview "Le llega esto"); la segunda se pasa a `enviarRecordatorioTurno()` (`lib/email/enviar.ts`) desde `recordatorio-actions.ts`. Vacía = el texto de siempre, sin romper nada para quien no la configure.
- **Exportar datos** — CSV de pacientes (nombre, contacto, DNI, obra social, sede, motivo de consulta, alta) generado en un Server Action (`exportarPacientesCsv`, discriminated union de estado como el resto del repo — no `throw`) y descargado client-side con `Blob`/`<a>` temporal. Deliberadamente NO incluye historia clínica/laboratorios/planes — dato clínico más sensible, ameritaría su propia pantalla con confirmación aparte.
- **Planes alimentarios** — `plantilla_plan_alimentario` precarga el textarea de "Escribir manualmente" en `PlanIAPanel` (ficha de paciente) — el profesional la sigue editando libremente antes de guardar/enviar, es un punto de partida, no un plan final.
- **Chat del portal** — `mensaje_bienvenida_chat` reemplaza el texto genérico del chat 1:1 vacío del paciente (`portal/chat/chat-view-paciente.tsx`) — solo en la conversación 1:1, un grupo vacío sigue con el texto genérico (no tiene sentido un "mensaje de bienvenida" por grupo).
- **Composición corporal** — `metricas_personalizadas` (`text[]`, tags como en "Especialidad" de Cuenta) es **solo el catálogo** — todavía NO está wireado a Historia Clínica/Mediciones (que hoy solo cargan peso). Usar estas métricas ahí tocaría el form de mediciones y el sparkline ya construidos — más grande que una pantalla de Configuración, queda como follow-up aparte, dicho explícitamente en la UI.
- **General** — edita `profesionales.consultorio`, que ya existía desde 001 (se usa como subtítulo del sidebar) pero nunca había tenido una pantalla propia para editarlo fuera del alta.
- **Siguen como "Próximamente"** (catch-all `[seccion]/page.tsx`, descripciones actualizadas en `secciones.ts`): **Notificaciones y app** (no hay ningún envío automático/programado en el proyecto — nada real que una preferencia pudiera controlar), **Reservas online** (feature pública nueva, no una pantalla de settings), **Cobros — config de moneda/métodos** (agregar esto tocaría `formatoMoneda` en muchos lugares para una preferencia sin consumidor hoy — distinto del módulo Cobros en sí, que ya está construido), **Plantillas de certificados** (necesita infra de generación de PDF, no existe en el proyecto), **Equipo** y **Suscripción** (decisiones de arquitectura/producto, sin cambios respecto a lo ya acordado).
- **Migración 013 aplicada y verificada** (`sedes`/`obras_sociales` existen con su policy `for all` cada una, las 6 columnas nuevas de `profesionales` confirmadas en `information_schema.columns`, vía `psql`/pooler).
- **`pre-commit-orchestrator` (mismo workaround) encontró 1 hallazgo bloqueante real** — no de fuga de datos, funcional: `profesionales_select_self` (002) es `using (user_id = auth.uid())`, así que un paciente nunca la matchea — **cualquier select a `profesionales` desde una sesión de paciente devolvía 0 filas por RLS**, sin importar qué id pidiera el código. Esto hacía "Chat del portal" (mensaje de bienvenida) inalcanzable para su único consumidor, y ya rompía en silencio el nombre del profesional en `obtenerConversacionesPaciente` (`portal/chat/actions.ts`, preexistente desde el módulo Chat, enmascarado por el fallback `"Tu nutricionista"`). **`supabase/migrations/014_profesionales_select_paciente.sql`** — policy nueva y aditiva, `profesionales_select_paciente`: `id in (select profesional_id from pacientes where user_id = auth.uid())`. Es de fila completa (RLS no tiene granularidad de columna) — se acepta a propósito, a diferencia de `notas_generales`/`laboratorios.notas_profesional`: no hay nada en `profesionales` que sea sensible de ocultarle al propio paciente. **Aplicada y verificada** (`pg_policies` confirma las 4 policies de `profesionales` — las 3 de 002 intactas + esta nueva).
  - 3 hallazgos **importantes** de `revalidatePath` incompleto, aplicados: `actualizarComunicacion` ahora también revalida `/app/pacientes/nuevo` (consumidor de `plantilla_invitacion_whatsapp`); `actualizarPlanes` también `revalidatePath("/app/pacientes/[id]", "page")` (mismo patrón que `wizard-actions.ts` para revalidar un dynamic segment sin id puntual); `actualizarChatPortal` también `/portal/chat`.
  - Cap de 20 métricas en Composición corporal ahora también en el cliente (`composicion-corporal-view.tsx`) — antes solo recortaba server-side en silencio si se cargaban más de 20.
  - Descripción de "Mi agenda" en `secciones.ts` corregida (ya no menciona "disponibilidad"/"duración por defecto", que no se construyeron).

**Cobros** (RF-070/071 — sin mockup, se construyó contra lo ya documentado en `docs/requirements.md`/`docs/architecture.md`, no hubo que inventar alcance):
- **Sin migración nueva** — la tabla `cobros` y sus 3 policies (`select`/`insert`/`update` scoped por `profesional_id`, sin acceso de paciente a propósito, "fuera de alcance para el paciente en v1") ya existían desde 001-003, aplicadas hace tiempo, solo sin UI.
- `src/lib/billing.ts` — el stub `crearLinkDePago` que los docs ya mencionaban pero nunca se había creado. Ningún código lo llama: `/app/cobros` es 100% registro manual, sin botón de "cobrar online" en la UI — cuando se integre Mercado Pago (Fase 2 explícita del PRD), este archivo es el único punto de contacto a tocar.
- `src/lib/queries/cobros.ts` — `obtenerResumenCobros` (se **movió** acá desde `lib/queries/dashboard.ts`, que la definía solo porque la Bandeja de hoy la necesitaba antes de que existiera este módulo; ahora vive en su lugar natural, `dashboard.ts`/`app/page.tsx` la importan de acá) + `obtenerCobros` (listado completo, filtrable por estado, con la consulta asociada si tiene).
- `src/app/app/cobros/` — `actions.ts` (`crearCobro`, releyendo que el paciente y la consulta opcional sean del profesional antes de insertar; `marcarCobrado`; `obtenerConsultasDePaciente` para el `<select>` dinámico del diálogo), `nuevo-cobro-dialog.tsx` (handler manual con `router.refresh()` tras crear — mismo motivo que `NuevoGrupoDialog` del módulo Chat: un `useActionState` cuyo `open` derivara de `state.status` quedaría pegado en "success" y no dejaría reabrir el diálogo la próxima vez), `marcar-cobrado-button.tsx` (sí usa `useActionState` normal, sin el problema de reapertura — mismo patrón que `RecordarTurnoButton`), `cobros-filtros.tsx` (tabs pendiente/cobrado/todos por query param, mismo patrón que `pacientes-filtros.tsx`).

**Configuración** (sin RF asignado — mockup nuevo del usuario en `~/Documents/Pantallas/Configuracion/`, una sola captura de las 17 secciones que lista el menú del mockup — el resto no tenía diseño):
- Feasibility check antes de construir (a pedido explícito del usuario): de las 17 secciones, solo **Cuenta** tenía pantalla real. **Equipo** (multi-usuario por consultorio) y **Suscripción** (facturación real) no son solo "falta diseño" — tocan decisiones de arquitectura/producto (hoy el modelo es 1 cuenta de Google = 1 profesional sin excepción; no hay integración de cobro online en ningún lado del proyecto, `lib/billing.ts` sigue stub). Confirmado con el usuario: se construye Cuenta y el resto queda "Próximamente" en el sub-menú, Equipo/Suscripción incluidas, sin diseñar nada de más para esas dos.
- `supabase/migrations/012_configuracion_cuenta.sql` — `profesionales` gana `apellido`, `telefono`, `matricula_nacional`, `matricula_provincial`, `profesion`, `especialidades` (`text[]`), `avatar_url`, `firma_url`. Sin RPC ni policy nueva: `profesionales_update_self` (002) ya permite que el profesional actualice cualquier columna de su propia fila (a diferencia del paciente, que no tiene esa policy a propósito) — es un `alter table` liso. Bucket privado nuevo `profesional-archivos` (5MB, solo imágenes) para avatar/firma, policies de un solo rol (nadie más que el propio profesional necesita leer/escribir ahí, a diferencia de `chat-adjuntos`).
- `src/app/app/configuracion/` — `secciones.ts` (mapa compartido de las 17 secciones, con la descripción de por qué cada una no está construida — no un placeholder vacío), `layout.tsx` + `configuracion-nav.tsx` (sub-nav real por rutas, mismo patrón que `app-shell.tsx`/`portal-nav.tsx` — no tabs en memoria, cada sección tiene su URL) y `[seccion]/page.tsx` (catch-all que renderiza `Proximamente` para cualquier slug del mapa que no sea `cuenta`, `notFound()` para cualquier otro string). `cuenta/` es la única pantalla real: `actions.ts` (`actualizarCuenta` + `subirAvatar`/`subirFirma`, mismo bucket, comparten `subirArchivoPerfil`), `cuenta-view.tsx` (formulario controlado completo, especialidades como tags con `Enter`/`,` para agregar, inputs ocultos por tag para que `FormData.getAll("especialidades")` los junte).
- **Deviations del mockup, documentadas en el código**: "Apellido" no es obligatorio (a diferencia del mockup) porque `profesionales.nombre` ya venía como nombre completo desde el alta — exigirlo rompería cuentas existentes. "Profesión" es texto libre, no el `<select>` del mockup (no hay una lista fija definida en ningún doc). El selector de código de país del teléfono no se construyó (input simple). La tarjeta "Seguridad" reemplaza "Enviar email de recuperación de contraseña" por una nota explicando que el login es 100% Google OAuth (`src/app/login/`) — no existe una contraseña propia de NutrIA que resetear, construir ese flujo tal cual el mockup habría sido una pantalla que no hace nada real.
- **Aplicada y verificada** (8 columnas nuevas confirmadas en `information_schema.columns`, bucket `profesional-archivos` privado con límite 5MB y mime types correctos, 2 policies de `storage.objects`, todo vía `psql`/pooler).
- **`pre-commit-orchestrator` (mismo workaround, ninguna definición recargada en esta sesión)** corrió sobre el módulo: tsc/lint/build limpios, RLS sin hallazgos (confirmó que `profesionales_update_self` ya cubre las columnas nuevas de fila completa, y que el path de Storage se arma 100% server-side con `profesional.id` del guard, sin ningún id externo de por medio). 3 hallazgos de seguimiento, los 3 aplicados:
  - **Real, no teórico** (confirmado contra el runtime de React 19 instalado en `node_modules`): `SubirArchivo` en `cuenta-view.tsx` llamaba al dispatcher de `useActionState` directo desde `onChange`, fuera de una transición — React exige `startTransition` para que `pending` se actualice y para no tirar `console.error("called outside of a transition")` en cada subida. Envuelto en `startTransition(() => formAction(fd))`.
  - `revalidatePath("/app")` en `cuenta/actions.ts` usaba el tipo default `'page'`, que no invalida el layout compartido (`app-shell.tsx`, donde vive el nombre/avatar del sidebar) — pasó a `revalidatePath("/app", "layout")` en los 2 lugares. Funcionaba igual hoy por un comportamiento de Next 16 documentado como temporal; esto ya no depende de eso.
  - `subirArchivoPerfil` no limpiaba el archivo de Storage si el `update` de `profesionales` fallaba después de subirlo — mismo gap que ya se había cubierto en el módulo de Chat (`enviarMensaje`/`enviarMensajePaciente`), ahora aplicado acá también (best-effort `remove()`, no cambia la respuesta si también falla).

**Chat** (RF-050/083, a partir del mockup `~/Documents/Pantallas/Chat/Chat1.png` — bastante más alcance que el RF-050 original de `docs/requirements.md`, que solo pedía "Mensajería 1:1... polling simple en v1"):
- `mensajes` (1:1) ya existía desde 001/002, sin UI. `supabase/migrations/011_chat_grupos_adjuntos.sql` suma lo que pedía el mockup y no estaba: **chat de grupo** (`chat_grupos` + `chat_grupo_miembros`, un grupo real donde los miembros se ven entre sí — es la semántica esperada de "grupo", no un bug de RLS) y **adjuntos** (`archivo_url`/`archivo_nombre`/`archivo_tipo` en `mensajes`, bucket privado `chat-adjuntos` sin whitelist de mime types, mismo límite de 15MB que `laboratorios`). `mensajes.paciente_id` pasa a nullable con un check (`(paciente_id is not null) <> (grupo_id is not null)`) — un mensaje es de un destino u otro, nunca los dos. `remitente_paciente_id` identifica qué paciente mandó un mensaje de grupo (en 1:1 ya lo dice `paciente_id`). Las 4 policies de `mensajes` de 002 quedan intactas — se sumaron policies nuevas para la rama de grupo (Postgres combina policies permisivas del mismo comando con OR, no hizo falta tocar las de 1:1). **Sin lectura por miembro en mensajes de grupo** (`leido` es un booleano por mensaje, no por destinatario — precisa una tabla de lecturas aparte que no se construyó en este pase, deliberado). **Aplicada y verificada** (`chat_grupos`/`chat_grupo_miembros` con 3 policies cada una, `mensajes` con 9 (6 originales de 002 + 3 nuevas de grupo), las 6 columnas nuevas confirmadas nullable, bucket `chat-adjuntos` privado con límite 15MB y 4 policies de `storage.objects`, todo vía `psql`/pooler).
  - **Bug real en la v1 de este archivo, corregido antes de aplicar**: la policy `chat_grupos_select_miembro` referenciaba `chat_grupo_miembros` antes de que esa tabla existiera en el mismo script — `psql` sin transacción explícita ya había commiteado `chat_grupos` + 1 policy cuando rompió ahí. Se relanzó una vez limpiado ese resto (`drop table chat_grupos cascade`) y con el archivo corregido (ambas tablas se crean primero, RLS y policies de las dos van después) **envuelto en `BEGIN`/`COMMIT` explícito** — si algo vuelve a fallar a mitad de camino, no queda nada a medio aplicar. Mismo patrón recomendado para migraciones futuras de más de una tabla nueva.
  - **`pre-commit-orchestrator` (workaround `general-purpose` con las 5 definiciones pegadas en el prompt — ninguna recargada en esta sesión)** corrió sobre el módulo entero: tsc/lint/build limpios, sin hallazgos bloqueantes de RLS (confirmó que las policies de grupo son aditivas, que el string-match exacto de `storage.foldername(name)[1]` no se puede bypasear, y que `remitente_paciente_id` no se puede spoofear vía FormData) y 2 hallazgos de seguimiento aplicados: **archivo huérfano en Storage** si el `insert` en `mensajes` falla después de subir el adjunto — `enviarMensaje`/`enviarMensajePaciente` ahora lo borran (best-effort) en ese caso; y **selects sin loguear `error`** en `obtenerConversaciones`/`obtenerConversacionesPaciente` — agregado.
- `src/lib/queries/chats.ts` (`obtenerConversaciones`) + `src/app/app/chats/actions.ts` — lista de conversaciones (1:1 + grupos) con último mensaje/adjunto y no-leídos, ordenada por actividad reciente ("status de cuándo se interactuó por última vez" del pedido, no presencia/online en vivo — decisión deliberada, ver abajo). `enviarMensaje` sube el adjunto a Storage antes de insertar (releyendo que el destino sea propio antes de subir, para no gastar la subida si el insert de todos modos va a fallar por RLS) y `crearGrupo` inserta el grupo + sus miembros en dos pasos (sin RPC atómica — no hay un cálculo cross-tabla que proteger más allá de lo que ya hace RLS, mismo criterio que `docs/architecture.md` para no-RPC).
- `src/app/app/chats/` — `chats-view.tsx` (dos paneles: lista con buscador por nombre + `NuevoGrupoDialog`, y la conversación activa), `message-bubble.tsx` (linkify de URLs en el texto, preview de imagen o chip de archivo para adjuntos, doble check de leído estilo WhatsApp), `query-provider.tsx` (TanStack Query — polling cada 4s en la conversación activa y cada 10s en la lista, primer uso real de la librería en el repo fuera de la dependencia declarada; **polling, no Realtime**, siguiendo la decisión ya documentada en `docs/architecture.md`). `NuevoGrupoDialog` usa un handler `async` manual en vez de `useActionState` + `<form action={fn}>` + `useEffect` — cerrar el diálogo y avisar al padre justo cuando la action resuelve, hecho desde un `useEffect` leyendo el estado, dispara el lint de React (`Calling setState synchronously within an effect`) porque reabrir el diálogo después reusaría el mismo estado "success" ya resuelto; con `await` directo en el handler el cierre pasa en el mismo evento que originó el submit.
- `src/app/portal/chat/` (RF-083) — mismo backend, UI más simple (sin buscador ni creación de grupos — el paciente no gestiona membresía). Reusa `MessageBubble` y `QueryProvider` de `src/app/app/chats/` (mismo patrón ya establecido de reusar componentes entre `/app` y `/portal`, ver `LoginForm` en `onboarding/invitacion/[token]/page.tsx`). Nav item nuevo en `PortalNav`.
- **Deliberadamente afuera de este pase** (no estaban en el pedido en texto del usuario, solo visibles como decoración en el mockup): la asignación "Chatbot"/"Atención humana" (auto-respuesta por IA — una feature real de agente, no de chat, con consideraciones de seguridad/responsabilidad médica que ameritan su propia conversación antes de construirla) y el rediseño completo de la sidebar que muestra el mockup (Biblioteca/Pagos/Catálogo/Página pública en vez de Alimentos/Cobros/Configuración — es un mockup de una versión más madura del producto, no algo que este pedido haya cubierto). "Status de última interacción" se interpretó como el timestamp del último mensaje por conversación (ya visible, ordena la lista), no presencia/online en vivo — eso necesitaría un canal de Realtime con heartbeat, fuera del alcance de "polling simple" que ya elegimos para todo lo demás del chat.

**Alta de paciente enriquecida** (RF-020, a partir de mockups nuevos del usuario en `~/Documents/pantallaNutri*.jpeg` — "Alta de paciente" con 3 vistas: "Lo cargo yo" / "Lo llena el paciente" / "En la ficha"):
- `supabase/migrations/008_perfil_paciente.sql` — `pacientes` gana campos opcionales (dni, obra_social, motivo_consulta, sede, quien_derivo, sexo_biologico) + tres bloques nuevos que llena el paciente desde el wizard (antecedentes de salud: condiciones/alergias/medicacion; hábitos y actividad: habitos_comidas/habitos_quien_cocina/habitos_movimiento; consentimiento_datos) + 5 columnas `*_completado_at` (una por sección, incluye datos_personales y contacto) que son la fuente de la completitud — no se infiere de la sola presencia de datos, porque el profesional puede precargar nombre/teléfono al crear sin que cuente como "sección completada por el paciente". Tabla nueva `invitacion_eventos` (log de "Actividad del link": enviado_whatsapp/abierto/seccion_completada). **Aplicada y verificada** (18/18 columnas nuevas confirmadas en `information_schema.columns`, `to_regclass('public.invitacion_eventos')` resuelve, vía `psql`/pooler).
- `supabase/migrations/009_rpc_perfil_paciente.sql` — dos RPCs nuevas, mismo criterio que `aceptar_invitacion` (SECURITY DEFINER + columnas whitelisteadas a mano, nunca update dinámico desde las claves del jsonb, para no reabrir la policy de update del paciente que 002 dejó deliberadamente afuera): `completar_seccion_perfil(seccion, datos)` (guarda una sección + marca `*_completado_at` + inserta el evento `seccion_completada`) y `registrar_evento_invitacion(token, tipo)` (log de `enviado_whatsapp`/`abierto`, bearer-auth por token porque "abierto" pasa antes de que el paciente tenga sesión — `enviado_whatsapp` sí valida que el profesional que llama sea el dueño de la invitación). **Aplicada y verificada** (`pg_proc` confirma las dos funciones).
- `supabase/migrations/010_aceptar_invitacion_idempotente.sql` — `aceptar_invitacion` pasa a ser idempotente: el mismo link se reabre más de una vez (wizard incompleto, o "Reenviar solo lo pendiente" desde la ficha) y antes el segundo request tiraba "ya tiene una cuenta vinculada" porque el update calificaba `where user_id is null`. Ahora, si la invitación ya estaba aceptada, es un no-op válido siempre que siga siendo la misma cuenta de Google — si no matchea, sigue siendo un error real. **Aplicada y verificada** (`pg_proc.prosrc` confirma la rama nueva).
- **Nota para el futuro**: `source .env.local` desde bash rompe — `RESEND_FROM_EMAIL` tiene el default `NutrIA <onboarding@resend.dev>` sin comillas, y bash lee `<`/`>` como redirección. Para leer una sola var desde ese archivo (ej. `SUPABASE_DB_PASSWORD` para `psql`), usar `grep '^VAR=' .env.local | cut -d= -f2-`, no `source`.
- `src/app/app/pacientes/nuevo/` — reemplaza al diálogo "Nuevo paciente" (`nuevo-paciente-dialog.tsx`, borrado) por una página completa con tabs "Lo cargo yo" (form real: nombre/email/teléfono + campos opcionales en chips progresivos, columna derecha con "Qué incluye el link" y preview "Le llega esto") y "Lo llena el paciente" (preview estático, no interactivo, del paso 1 del wizard — es una vista previa para el profesional, no un flujo funcional duplicado). **Deviation deliberada**: el mockup combina "Teléfono o email" en un campo único; se mantuvo el email separado y obligatorio porque `aceptar_invitacion()` matchea la invitación contra ese email exacto de la cuenta de Google — combinarlos rompería esa validación. La tercera vista del mockup ("En la ficha") no es un tab acá — se integró directo en la ficha real del paciente (ver abajo), porque muestra completitud de un paciente ya existente, no tiene sentido antes de crearlo.
- `src/app/onboarding/invitacion/[token]/perfil-wizard.tsx` + `wizard-actions.ts` — después de `aceptar_invitacion`, si quedan secciones con `*_completado_at` nulo, se muestra el wizard (Card standalone, mismo estilo que la pantalla de login) en vez de redirigir directo a `/portal`. Resume automáticamente en la primera sección pendiente y guarda paso a paso (no todo junto al final) — si el paciente cierra el link a mitad de camino, lo ya respondido queda guardado. Esto reemplaza la necesidad de un link con `?paso=X`: "Pedir" una sección puntual y "Reenviar solo lo pendiente" mandan el mismo link sin parámetros, total el wizard ya sabe dónde retomar.
- `src/app/app/pacientes/[id]/page.tsx` — nueva sección "Completitud del perfil" (antes de Historia clínica): % + checklist de las 5 secciones con fecha o botón "Pedir" (`pedir-seccion-button.tsx`, wa.me con el link de invitación, logueando `enviado_whatsapp` vía RPC en el click), panel "Pedir lo que falta" (chips de las pendientes + "Reenviar solo lo pendiente") y "Actividad del link" (últimos `invitacion_eventos`). El link absoluto se arma server-side con `headers()` (`host`), no con `window.location`, para no depender de un mount en cliente.
- Simplificación deliberada vs. el mockup: no se implementó el botón "Crear y enviar el link" con auto-apertura de WhatsApp al crear — `window.open()` disparado desde un `useEffect` después de que resuelve la Server Action casi siempre lo bloquea el popup blocker del navegador, porque para entonces ya no hay gesto de usuario síncrono. "Crear paciente" queda como una sola acción; el envío por WhatsApp siempre es un click explícito aparte (mismo patrón que ya usaba el diálogo viejo).
- **QA del agente (workaround `general-purpose` con la definición de `qa-tester.md` pegada en el prompt, la sesión no lo tenía recargado)** encontró y se corrigieron 3 hallazgos, ninguno bloqueante:
  - **Importante**: en `alta-paciente-view.tsx`, el input de `email` y los 5 inputs opcionales (dni/obra_social/motivo_consulta/sede/quien_derivo) estaban sin controlar dentro de `<form action={formAction}>` — mismo gotcha ya documentado de Agenda (React 19 resetea campos no controlados al terminar la transición, incluso en un resultado de error, y `crearPaciente` nunca lanza). Fix: todos controlados (`value`/`onChange`), mismo criterio que `nombre`/`telefono`.
  - Menor: la alerta de "sin registrar comidas" en el Inicio solo mostraba el paciente más atrasado sin indicar si había más — ahora suma `· +N más` cuando corresponde, igual que el resto de las alertas de la bandeja.
  - Menor: el fallback defensivo de `perfil-wizard.tsx` (`seccionesPendientes.length === 0`, inalcanzable hoy porque `page.tsx` ya redirige antes de montar el wizard) llamaba `router.replace()` directo en el cuerpo del componente — movido a `useEffect` para no disparar una navegación como side effect durante el render.

**Paleta y tipografía ya son las del diseño real** (no un placeholder): sidebar violeta/ciruela oscuro (`--sidebar`), fondo crema (`--background`), acento naranja/coral (`--accent`, `--destructive` para prioridad ALTA), heading serif con Google Font Lora (`--font-heading`, aplicado a h1/h2/h3 vía `@layer base`) — todo en `globals.css`. Los valores se estimaron a ojo de las capturas (no hay extractor de color exacto) — si algo se ve desalineado con el diseño original, es candidato a ajuste fino, no a rediseño.

**Pantallas construidas** (`src/app/app/`):
- `components/app-shell.tsx` — sidebar fijo con nav (Inicio/Pacientes/Agenda/Chats + sección Contenido: Planes/Alimentos/Recursos/Formularios + Configuración), estado activo por ruta, menú de usuario (dropdown) con logout.
- `/app` (Bandeja de hoy, RF-040/041) — alertas reales calculadas con queries, no hardcodeadas: pacientes activos sin próximo turno, turnos pendientes en las próximas 48h, cobros pendientes, laboratorios sin revisar hace más de 48h y (agregado después, ver `obtenerPacientesSinRegistrarComida` en `lib/queries/dashboard.ts`) pacientes dados de alta hace más de 7 días sin registrar comida en los últimos 7 — prioridad nueva `INFO` (sin dot/label, más sutil que ALTA/MEDIA) para esta última, matcheando el mockup. **Deliberadamente sigue sin mostrar** adherencia/recuperados agregados — se documentó como nota visible en el panel de métricas en vez de fabricar números. Métricas reales: % continuidad, $ por cobrar. Agenda de hoy con brief de continuidad (de `consultas`, solo si existe una fila real). Actividad reciente combina `mediciones`/`planes.enviado_at`/`turnos.created_at` (no incluye "turno confirmado" porque el schema no registra cuándo cambió el estado, solo cuándo se creó la fila).
- `/app/pacientes` (RF-020/021) — listado con búsqueda/filtros por query params (`?q=&estado=&sin_turno=`), banner de alerta reusando la misma query que la Bandeja de hoy (`lib/queries/pacientes.ts`), diálogo "Nuevo paciente" que llama a la RPC `invitar_paciente` y muestra el link de invitación para copiar + botón "Enviar por WhatsApp" (`wa.me`/`api.whatsapp.com`, con el teléfono cargado si hay o dejando elegir el contacto si no) — es el canal principal de invitación, no mail (ver más abajo por qué).
- `/app/agenda` (RF-030/032) — calendario (mes, `react-day-picker`) + lista de turnos del día seleccionado, traídos de una sola vez (últimos 30 días / próximos 120) y filtrados en el cliente al cambiar de día, sin ida y vuelta al server. Crear/editar turno (paciente, fecha, hora, tipo, notas — reasignar paciente no está en el alcance de "editar"), cambiar estado a mano (`pendiente/confirmado/en_curso/cancelado`, `<select>` nativo con auto-submit `onChange` — no el `Select` de shadcn/Base UI, para no lidiar con su timing async para un caso tan simple), y "Registrar brief" (RF-032, crea una fila en `consultas`). `/app/agenda?paciente=<id>` (ya linkeado desde el botón "Agendar" del listado de Pacientes) abre directo el diálogo de crear con ese paciente fijo.
  - **Fecha/hora — por qué se arma en el cliente**: el form manda un ISO ya calculado con `new Date(\`${fecha}T${hora}\`)` en el navegador del profesional, no `fecha`+`hora` sueltos para que el server los combine — Netlify corre las funciones en UTC, así que reconstruir la fecha en el servidor la interpretaría en UTC en vez de en la zona horaria real del profesional, corriendo el turno varias horas. Ver `turno-form-dialog.tsx` y el comentario en `agenda/actions.ts`.
  - **Integración con Google Calendar — decisión deliberada, no a medias**: se evaluaron dos alcances (ver la conversación que lo definió) y se eligió el más simple a propósito — `src/lib/calendar.ts` arma un link `calendar.google.com/calendar/render` con el evento precargado ("Agregar a Google Calendar" en cada turno), sin OAuth nuevo ni tokens guardados. **No sincroniza** — editar/cancelar un turno después no actualiza el evento ya agregado. El sync real de dos vías (crear/editar/borrar el evento solo) quedó evaluado y descartado para este alcance: requeriría pedir el scope `calendar.events` en el login (re-consentimiento de todos los profesionales ya logueados), guardar su refresh token, y muy probablemente una verificación de Google más estricta que la actual (`docs/architecture.md` §4 ya documenta la verificación de la app OAuth actual como el ítem de mayor lead time externo del roadmap — sumar el scope de Calendar la haría más estricta todavía).
  - **Bugs reales encontrados por el agente de QA en este módulo, corregidos** (ninguno lo agarraba build/lint):
    - `TurnoFormDialog` estaba siempre montado en `agenda-view.tsx` (solo alternaba la prop `open`) — el `useActionState` de adentro sobrevivía al cierre, así que crear dos turnos seguidos (o editar el mismo turno dos veces) sin pasar por otra identidad de por medio dejaba el diálogo pegado en la pantalla de "Turno creado/actualizado" de la vez anterior en vez de mostrar el form vacío. Fix: montarlo condicionalmente (`{(crearAbierto || turnoEditando) && <TurnoFormDialog .../>}`), mismo patrón que ya usaba `BriefDialog` al lado.
    - `pacienteFijo` (de `/app/agenda?paciente=<id>`) quedaba fijo para siempre después de la primera apertura — el botón genérico "Nuevo turno" no lo limpiaba, así que agendar a otro paciente después de entrar por ese link seguía mostrando el paciente viejo. Fix: `pacienteFijoActivo` como estado propio de `AgendaView` (no la prop directo), que el botón genérico resetea a `undefined` antes de abrir.
    - **React 19 resetea los campos no controlados de un `<form action={...}>` al terminar la transición de la action, incluso cuando el resultado es un error** — y las Server Actions de este proyecto nunca lanzan (`{status: "error"|"success"}`), así que ese reset nunca se puede esquivar devolviendo un error. Afectaba a tres campos que habían quedado no controlados por descuido (el resto del proyecto ya usa inputs controlados): el `<select>` de estado del turno (`estado-turno-select.tsx`, podía "saltar" a un valor viejo), y los campos de `registro-form.tsx`/`brief-dialog.tsx` (podían vaciarse justo al mostrar un error de validación). Los tres pasaron a controlados — no es un caso aislado, es un gotcha general de la convención `<form action={fn}>` + acciones que nunca lanzan: cualquier campo nuevo en ese patrón necesita `value`/`onChange`, no `defaultValue`.
- `/app/{planes,alimentos,recursos,formularios}` — stubs "Próximamente" (`components/proximamente.tsx`) para que ningún link del nav ni de la Bandeja de hoy dé 404. `/app/chats`, `/app/cobros` y `/app/configuracion/cuenta` ya no son stubs — ver sus secciones más abajo (`/app/configuracion/*` retiene 16 de 17 sub-secciones como "Próximamente", ver "Configuración").

**Invitación a paciente por WhatsApp, no por mail** (RF-020): se probó el envío automático por Resend y falló contra un paciente real — confirmado contra la API real (no solo compilado) que la cuenta de Resend está en modo sandbox y solo entrega al email dueño de la cuenta (`ijproyectos26@gmail.com`), a cualquier otro destinatario le devuelve 403 sin dominio verificado. Se sacó el envío de mail de `crearPaciente` (`pacientes/actions.ts`) y en su lugar `nuevo-paciente-dialog.tsx` tiene un botón "Enviar por WhatsApp" (`wa.me`/`api.whatsapp.com`) al lado del link para copiar. Si se verifica un dominio propio en Resend en el futuro, volver a esto es directo — el criterio "nunca lanza" de `src/lib/email/enviar.ts` sigue ahí.

**Email transaccional con Resend** (queda solo para RF-042, recordatorio manual de turno — `docs/architecture.md` §, sección "Email transaccional"):
- `src/lib/email/enviar.ts` — único punto de contacto con `resend` (`RESEND_API_KEY` en `.env.local`, `import "server-only"`). Mismo criterio "nunca lanza" que `src/lib/laboratorios/parsear.ts`: si el envío falla, devuelve `{ enviado: false, error }` en vez de tirar.
- `app/recordatorio-actions.ts` + `app/recordar-turno-button.tsx` — botón "Recordar a {paciente}" por turno sin confirmar, directo en la Bandeja de hoy (alerta de "turnos sin confirmar en las próximas 48h"). El turno se relee server-side por RLS antes de mandar el mail — no confía en lo que venga del form.
- **Mismo límite de sandbox que la invitación** (ver arriba): un recordatorio a un paciente real también va a fallar hasta que se verifique un dominio en Resend. Queda como está (no se pidió moverlo a WhatsApp) — si hace falta, mismo criterio que la invitación.

**Notas de lint que importan para código futuro en este repo**: eslint-config-next trae las reglas nuevas de React Compiler (`react-hooks/purity`, `react-hooks/set-state-in-effect`). No usar `Date.now()`/`Math.random()` en el cuerpo de un Server Component — usar `new Date()` (sí lo tolera, ver `src/lib/queries/*.ts` y `src/lib/format.ts` para el patrón). No hacer `setState` directo dentro de un `useEffect` — mover la lógica al handler que dispara el cambio (ver `nuevo-paciente-dialog.tsx`). No usar `window.location.href` para navegación interna — `useRouter()` de `next/navigation`. `<Button render={<Link .../>}>` (o cualquier `render` que no sea un `<button>` de verdad, ej. `<a>`/`Link`) necesita `nativeButton={false}` explícito — sin eso Base UI tira un console error en runtime ("expected a native <button>"), no se detecta en build/lint (bug real encontrado en `auth-code-error/page.tsx` y `onboarding/invitacion/[token]/page.tsx`, corregido). Cualquier campo dentro de un `<form action={fn}>` tiene que ser controlado (`value`/`onChange` o `checked`/`onCheckedChange`), no `defaultValue`/no controlado — React 19 resetea los campos no controlados de un form al terminar la transición de la action, sin importar si el resultado fue éxito o error, y como la convención de este repo es que las actions nunca lanzan (devuelven `{status: "error"}` en vez de tirar), ese reset no se puede esquivar. Bug real encontrado por el agente de QA en `estado-turno-select.tsx`/`registro-form.tsx`/`brief-dialog.tsx`, corregido — ver la nota en la sección de Agenda.

**Supabase está vivo**: proyecto ref `mgscprkbdbwejfhvkggm` (región `us-east-1`), URL `https://mgscprkbdbwejfhvkggm.supabase.co`. Las migraciones 001 a 010 están **aplicadas y verificadas** contra la DB en vivo (10 tablas base + `invitacion_eventos` de 008, RLS habilitado en todas, funciones `auth_profesional_id`, `auth_paciente_id`, `invitar_paciente`, `aceptar_invitacion`, `confirmar_turno`, `completar_seccion_perfil`, `registrar_evento_invitacion` — más el fix de `invitar_paciente` de 006, `pacientes.notas_generales` de 007, y el perfil de paciente enriquecido de 008-010). Aplicadas vía `psql` por el session pooler (`aws-0-us-east-1.pooler.supabase.com:5432`, user `postgres.mgscprkbdbwejfhvkggm`) — no hay Supabase CLI instalado, `psql` viene de `libpq` sin linkear al PATH (`find /opt/homebrew/Cellar/libpq -name psql` si cambia la versión). La contraseña del pooler está en `.env.local` (raíz, gitignored) como `SUPABASE_DB_PASSWORD` — a pedido explícito del usuario, aunque la app en sí no la lee (es solo para `psql`/migraciones). Si en algún momento no está o cambió, viene de Project Settings → Database → Connection string, o reseteándola ahí. No re-aplicar ninguna migración sin chequear el estado actual del proyecto primero.

**Trade-off de RLS sistémico, no nuevo de este pase** (notado al escribir `agenda/actions.ts`, pero ya estaba desde `mediciones`/`registros_comida`/`consultas`): las policies `*_insert_profesional` (`turnos_insert_profesional`, `mediciones_insert_profesional`, etc.) solo chequean `profesional_id = auth_profesional_id()` en el `with check` — ninguna valida que el `paciente_id` de la fila realmente pertenezca a ese profesional. Un Server Action que confíe en un `paciente_id` de `FormData` sin haberlo re-leído (a diferencia de `recordatorio-actions.ts`, que sí relee el turno) podría, en teoría, insertar una fila con `profesional_id` propio pero `paciente_id` de un paciente ajeno. Hoy no es explotable desde la UI (los `<select>` de paciente siempre listan solo los pacientes propios, ya filtrados por RLS de `pacientes`), pero es un endurecimiento pendiente si en algún momento se agrega un endpoint que acepte un `paciente_id` de una fuente menos controlada. Arreglo real si hace falta: `paciente_id in (select id from pacientes where profesional_id = auth_profesional_id())` en cada policy de insert, no parche por tabla.

Credenciales en `.env.local` (raíz, gitignored; `apps/web/.env.local` es symlink) — cargadas. Google provider activado en Supabase Auth y redirect URI agregado en Google Cloud Console (confirmado por el usuario, no verificable desde acá sin acceso a esos dashboards).

**Código de auth** (`npm run build`/`lint` limpios, y el usuario confirmó que el login con Google funciona en vivo — incluyó resolver un `redirect_uri_mismatch` corrigiendo el Authorized redirect URI en Google Cloud Console):
- `src/lib/supabase/{client,server}.ts` — clientes Supabase estándar SSR.
- `src/lib/dal.ts` — `getSession()`, `resolveRole()` (resuelve `profesional` / `paciente` / `sin_rol` en una sola query paralela, cacheada por request), `getAuthorizedProfesional()`/`getAuthorizedPaciente()` (guards de `/app` y `/portal`, redirigen cruzado si el rol no matchea en vez de rechazar — ver la nota en el archivo sobre por qué esto difiere del patrón allowlist de un proyecto de un solo tenant).
- `src/proxy.ts` — check optimista de cookie de sesión únicamente (sin query a rol), igual patrón que Raíces del Sur.
- `src/app/login/` — botón Google, forwardea `?next=` a través del callback.
- `src/app/auth/callback/route.ts` — solo intercambia el code, no decide rol (eso es `/` + `resolveRole()`).
- `src/app/onboarding/` — chooser (tabs) profesional/paciente; `actions.ts` tiene las dos Server Actions (`altaProfesional`, `aceptarInvitacionInput`); `invitacion/[token]/page.tsx` es el link directo que se le manda al paciente (auto-acepta si ya hay sesión).
- `src/app/portal/` — layout con la guard (`PortalNav`: Inicio/Mis turnos/Mi plan/Laboratorios/Registrar/Chat). RF-080 a RF-085 construidos: dashboard (próximo turno + confirmar, resumen del plan vigente, acceso a registrar), `/portal/turnos` (RF-082, listado propio + confirmar vía RPC `confirmar_turno`), `/portal/registro` (RF-081, form de comida/peso → `registros_comida`/`mediciones`), `/portal/plan` (RF-061), `/portal/laboratorios` (RF-084/085) y `/portal/chat` (RF-083, ver "Chat" más arriba). Nada del portal paciente queda como placeholder.

**Próximo paso real**: con Chat, Cobros y Configuración → Cuenta construidos, todos los módulos centrales del PRD original están. Queda: las pantallas de contenido (`alimentos`, `recursos`, `formularios`) que siguen como "Próximamente", y las 16 sub-secciones de Configuración sin diseño (ver esa sección) a medida que haya mockups — Equipo y Suscripción en particular necesitan una conversación de producto/arquitectura antes de construirse, no solo una captura.

## Qué es

NutrIA es un SaaS multi-tenant para nutricionistas en LATAM (self-serve: cualquiera se da de alta como profesional, sin allowlist) que centraliza pacientes, agenda, planes alimentarios, cobros y comunicación, con una "Bandeja de hoy" que prioriza alertas de continuidad. Dos frontales en el mismo Next.js app: panel profesional (`/app/*`) y portal paciente (`/portal/*`), separados por `proxy.ts` según a qué tabla pertenece el usuario logueado (`profesionales` vs `pacientes.user_id`).

Doc completo: `docs/product.md`. Requisitos funcionales (RF-xxx): `docs/requirements.md`. Esto fue armado a partir de un PRD/MVP entregado por el usuario (`docs/product-prd-original.md` y `docs/prompt-original.md`, conservados tal cual como referencia histórica) con una serie de recortes de alcance deliberados para v1 — están explicados con el porqué en `docs/product.md` y `docs/architecture.md` §6, no re-litigar sin revisar esa sección primero.

## Decisiones que difieren del PRD original (y por qué)

- **`profesional_id` denormalizado en toda tabla de negocio**, aunque también tenga `paciente_id` (`consultas`, `mediciones`, `registros_comida`) — el PRD original no lo tenía; se agregó para evitar policies RLS con subqueries anidadas.
- **`pacientes.user_id` en vez de una tabla de mapeo aparte** — el prompt original mencionaba una tabla `user_id -> paciente_id`; para MVP (1 paciente = 1 cuenta) alcanza una columna.
- **Alta de profesional self-serve, sin allowlist** — a diferencia de otros proyectos de un solo tenant, acá es el modelo de negocio: cualquiera se registra como profesional. El paciente sí sigue gateado por invitación (nunca se autorregistra).
- **Sin monorepo real** — el PRD lo sugería; no hace falta porque no hay backend propio, solo Supabase.
- Recortes de alcance para v1 (recordatorios manuales en vez de cron, planes en texto sin upload, sin CSV, sin auditoría) — confirmados con el usuario, detalle en `docs/product.md`.

## Stack

Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind CSS 4 + shadcn/ui (`base-nova`) + Lucide icons, sobre Supabase (Postgres, Auth, Row Level Security, Storage — bucket privado `laboratorios` en uso desde v1) + Anthropic API (`claude-opus-5`), hosted on Netlify (`netlify.toml`). Validación con Zod + React Hook Form. Server Components para lectura; TanStack Query solo donde haga falta cache/mutación del lado del cliente (ej. chat).

## Running the app

```bash
cd apps/web
npm run dev      # local dev, http://localhost:3000
npm run build    # production build
npm run lint     # eslint
```

**Next.js 16**: `middleware.ts` se llama `proxy.ts` (función exportada `proxy`). Antes de escribir código de App Router que no sea un tweak chico, revisar `apps/web/node_modules/next/dist/docs/` — el training data está desactualizado para lo que cambió Next 16.

## Docs map

- `docs/product.md` — visión, usuarios, alcance de v1 y qué queda fuera (con el porqué).
- `docs/requirements.md` — RF-xxx por módulo (auth, pacientes, agenda, dashboard, chat, planes, cobros, portal paciente).
- `docs/architecture.md` — stack, estructura de rutas, multi-tenancy/RLS, auth (self-serve profesional + invitación paciente), flujos críticos (RPCs), qué se difirió y por qué, infra recomendada.
- `docs/data-model.md` — entidades, campos, relaciones, reglas de integridad.
- `docs/product-prd-original.md` / `docs/prompt-original.md` — el PRD y el prompt tal cual los entregó el usuario, sin editar. Referencia histórica, no la fuente de verdad — para eso están los 4 docs de arriba.

## Supabase layout

- `supabase/migrations/001_initial_schema.sql`, `002_rls_policies.sql`, `003_rpc_funciones.sql` — escritas, **no aplicadas todavía** (no existe el proyecto Supabase). Aplicar en ese orden vía `psql` a través del session pooler cuando el proyecto exista (mismo mecanismo usado en Raíces del Sur — `db.<ref>.supabase.co` es IPv6-only e inalcanzable desde el sandbox, el pooler es el camino que funciona).
- `supabase/seed.sql` — no siembra `profesionales`/`pacientes` reales (requieren un `auth.users.id` real de un login real); tiene un ejemplo comentado para poblar pacientes de prueba una vez que exista un profesional real.
- Env vars (`.env.example`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (opcional, default `NutrIA <onboarding@resend.dev>`). Ninguna se expone al frontend — cargar las 6 en Netlify, no solo las 4 originales.
