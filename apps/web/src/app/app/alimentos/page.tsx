import { Proximamente } from "@/components/proximamente";

export default function AlimentosPage() {
  return (
    <Proximamente
      titulo="Alimentos"
      descripcion="Base de alimentos/macros — fuera de alcance del MVP (ver docs/product.md)."
    />
  );
}
