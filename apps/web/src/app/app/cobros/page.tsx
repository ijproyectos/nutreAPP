import { Proximamente } from "@/components/proximamente";

export default function CobrosPage() {
  return (
    <Proximamente
      titulo="Cobros"
      descripcion="Registro manual de cobros y reporte de por cobrar (RF-070/071) todavía no está construido."
    />
  );
}
