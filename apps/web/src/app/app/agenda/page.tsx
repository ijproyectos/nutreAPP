import { Proximamente } from "@/components/proximamente";

export default function AgendaPage() {
  return (
    <Proximamente
      titulo="Agenda"
      descripcion="El calendario de turnos (RF-030/031/032) todavía no está construido — próximo paso después de Pacientes."
    />
  );
}
