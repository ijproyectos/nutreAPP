import { Proximamente } from "@/components/proximamente";

export default function FormulariosPage() {
  return (
    <Proximamente
      titulo="Formularios"
      descripcion="Intake forms, anamnesis y consentimientos todavía no están construidos."
    />
  );
}
