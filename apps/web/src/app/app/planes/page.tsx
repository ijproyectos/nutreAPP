import { Proximamente } from "@/components/proximamente";

export default function PlanesPage() {
  return (
    <Proximamente
      titulo="Planes"
      descripcion="Crear y enviar planes alimentarios en texto (RF-060) todavía no está construido."
    />
  );
}
