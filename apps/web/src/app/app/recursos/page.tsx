import { Proximamente } from "@/components/proximamente";

export default function RecursosPage() {
  return (
    <Proximamente
      titulo="Recursos"
      descripcion="Materiales compartibles (PDF, videos, guías) todavía no están construidos."
    />
  );
}
